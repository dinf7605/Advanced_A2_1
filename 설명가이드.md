# 📘 설계 노트 — 우리는 왜 이렇게 만드는가

> A2-1 브랜드 아이덴티티 생성기를 **어떤 판단으로 이렇게 설계했고, 그 결과 무엇이 어떻게
> 조립되는지**를 처음부터 끝까지 따라가는 문서.

| 문서 | 답하는 질문 |
|------|-------------|
| [brand_kit_PRD.md](brand_kit_PRD.md) | **무엇을** 만들 것인가 — 명세·스키마·일정·역할 |
| **이 문서** | **왜 그렇게** 만드는가 — 판단의 근거와 데이터의 흐름 |

PRD는 만들면서 펴놓고 보는 문서이고, 이 문서는 **시작하기 전에 한 번, 끝나고 한 번** 읽는 문서입니다.

---

## 1. 출발점 — 이 과제가 실제로 요구하는 것

과제 문서를 그대로 읽으면 "AI로 브랜드 요소를 만드는 프로그램"입니다.
하지만 요구사항 10개를 뜯어보면 **진짜 난이도는 생성이 아니라 다른 곳**에 있습니다.

| 겉보기 요구 | 실제로 우리가 풀어야 하는 문제 |
|-------------|-------------------------------|
| "AI로 네이밍을 생성한다" | AI의 **자유로운 출력을 코드가 꺼내 쓸 수 있는 구조**로 만드는 것 |
| "이미지 생성 API로 로고를 만든다" | **텍스트를 뱉는 API와 바이너리를 뱉는 API**를 한 파이프라인에 엮는 것 |
| "컬러 팔레트를 시각화한다" | AI가 준 문자열이 **정말 색인지 믿을 수 없다는 것**을 전제로 그리는 것 |
| "실패해도 다음 단계를 계속 진행한다" | 실패를 **예외가 아니라 설계의 일부**로 다루는 것 |

여기에 이번 과제만의 조건이 하나 더 붙습니다 — **5명이 동시에 만든다.**
그래서 "혼자라면 하지 않았을 결정"이 몇 개 들어갑니다.

### 우리가 지금 어디까지 왔는가

| 과제 | 핵심 학습 | 실패의 성격 |
|------|-----------|-------------|
| A1-1 Prompt Manager | 자료구조 설계 | 내가 통제하는 실패 (사용자 입력 오류) |
| A1-2 Trip Planner | REST API, LLM 출력의 구조화, **API 체인** | 내가 통제 못 하는 실패 (네트워크·인증·쿼터) |
| **A2-1 Brand Kit** | **성질이 다른 두 API 조합 + 부분 실패 설계 + 팀 협업** | **일부만 실패하는 상태를 결과물로 만드는 것** |

A1-2는 체인이라 "앞이 끊기면 뒤가 없다"는 구조였습니다.
**A2-1은 단계가 서로 독립적이라, 처음으로 "절반만 성공한 결과물"이라는 상태를 다루게 됩니다.**
이게 이 과제의 중심입니다.

---

## 2. 요구사항의 어느 문장 때문에 무엇을 쓰는가 ★

**우리가 쓰는 도구는 취향으로 고른 게 아닙니다.** 요구사항의 특정 문장이 선택을 좁힌 결과입니다.
"왜 이걸 썼나요"라는 질문의 답은 대부분 이 표 안에 있습니다.

### 2.1 요구사항 → 도구·기법

| # | 요구사항의 문장 | 그래서 우리가 쓰는 것 | 다른 선택을 안 하는 이유 |
|:-:|-----------------|----------------------|------------------------|
| 1 | "**print와 input을 사용하여** 대화형으로 입력받는다" | 표준 `input()` + `while` 루프 | **A1-2에서 쓴 `argparse`를 여기선 못 씁니다.** argparse는 1회 실행형이라 "대화형"과 구조가 충돌합니다 |
| 1 | "필수 입력: 브리프 경로 / 선택 입력: 출력 폴더(기본 `./output`)" | 필수는 `while` 재입력 루프, 선택은 `input() or "./output"` | 필수는 값이 없으면 진행 불가, 선택은 **빈 값이 곧 유효한 답**(기본값 사용)이라 처리가 다릅니다 |
| 2 | "**JSON 파일로** 브랜드 정보를 입력받는다" | 표준 `json` 모듈 | 외부 파서 불필요 |
| 2 | "필수 필드: `industry` `target` `keywords`" | **직접 만든 검증 함수** | `jsonschema` 라이브러리는 필드 3개 확인에 의존성 추가가 과합니다. 대신 `JSONDecodeError`의 `lineno`·`colno`를 살려 **몇 번째 줄이 틀렸는지** 알려줍니다 |
| 3~6 | "**LLM API를 호출하여**" (네 요건에 각각 등장) | `google-genai` (Gemini) — **요건마다 별도 호출** | 요건이 항목별로 나뉘어 있고, 한 번에 받으면 요건 9와 충돌합니다 (§6) |
| 3 | "각 네이밍에 대한 **의미/유래 설명을 함께**" | 스키마에 `meaning` 필드를 못 박음 | 산문으로 받으면 이름과 의미를 코드로 분리할 수 없습니다 |
| 4 | "브랜드 **톤앤매너에 맞는** 문구" | 스키마에 `tone_note` 필드 (톤을 어떻게 반영했는지) | "맞는지"를 사람이 눈으로만 판단하면 검증이 안 됩니다. **모델에게 근거를 같이 내게 해서 확인 가능하게** 만듭니다 |
| 5 | "브랜드 스토리(**300자 내외**)" | `len(text)`를 재서 `char_count`로 저장 | 요건의 숫자는 **결과물에 기록으로 남아야** 확인됩니다 |
| 5 | "**탄생 배경, 철학, 비전을 포함**한다" | 스키마에 세 필드를 따로 둠 | 산문 한 덩어리로 받으면 세 요소가 들어갔는지 확인할 방법이 없습니다 |
| 6 | "메인 1개, 서브 2~3개를 **HEX 코드로** 받는다" | 정규식 `^#[0-9A-Fa-f]{6}$` 검증 + 스키마 `minItems`(하한만) | **"HEX로 달라"는 요구가 곧 "HEX가 아닐 수 있다"는 뜻**입니다. 스키마는 문자열인지만 보므로 `"deep green"`이 통과합니다. **개수 상한은 스키마에 두지 않습니다** — 초과분은 잘라내면 요건을 충족하는데, 검증으로 탈락시키면 쓸 수 있는 응답을 버리게 됩니다 |
| 6 | "**matplotlib 등** 다양한 방법으로 시각화하여 PNG 저장" | **matplotlib** + `Agg` 백엔드 + `savefig` | "등"이라 Pillow로 직접 그려도 되지만, **사각형·텍스트·폰트·저장이 한 번에** 됩니다. Pillow는 좌표를 전부 손으로 계산해야 합니다 |
| 7 | "**이미지 생성 API(DALL-E 등)** 를 호출하여" | `openai` SDK 또는 Gemini 이미지 — **어댑터로 교체 가능** | **"등"이 열어둔 여지를 그대로 씁니다.** 유료라 막힐 수 있어서 갈아끼울 수 있게 만듭니다 |
| 7 | "생성된 이미지를 **PNG 파일로** 저장" | `open(path, "wb")` + base64 디코딩 또는 URL 다운로드(`requests`) | 이미지는 텍스트가 아니라 `"w"`로 열면 깨집니다. 응답이 URL이면 **다운로드가 한 단계 더** 필요합니다 |
| 8 | "텍스트 결과는 **`brand_result.json`** 으로 저장" | `json.dump(..., ensure_ascii=False, indent=2)` | **파일명이 요건에 지정돼 있으므로 바꾸지 않습니다.** `ensure_ascii=False`가 없으면 한글이 `이음` 형태로 저장돼 사람이 못 읽습니다 |
| 8 | "출력 폴더에 모든 결과물을 저장" | `os.makedirs(out_dir, exist_ok=True)` | 폴더가 없으면 만들고, 있으면 그냥 씁니다 |
| 9 | "API 호출 실패 시 에러 메시지를 출력하고 **다음 단계를 계속 진행**" | **예외 대신 반환값 계약** + 공유 `errors` 리스트 | `try/except`를 호출부마다 두면 5명 중 누군가는 빠뜨립니다. **계약으로 바꾸면 빠뜨릴 자리가 없습니다** (§8) |
| 9 | "**API 키가 없거나 잘못된 경우** 명확한 안내 메시지" | 호출 **전에** 키 존재 확인 + `401`을 별도 분기 | 키가 없는 것과 틀린 것은 사용자가 할 일이 다릅니다 — 하나는 발급, 하나는 확인 |
| 10 | "API 키를 **코드에 직접 작성하지 않는다**" | `python-dotenv` + `.env` + `.env.example` | `os.getenv`만 써도 요건은 충족되지만, **5명이 각자 키를 넣어야 하는데 OS마다 환경변수 설정법이 다릅니다.** `.env` 파일 하나면 5명이 같은 방법을 씁니다 |
| B1 | "입력된 경쟁사를 분석하여 **차별화 포인트를 제안**" | 브리프의 선택 필드 `competitors`가 있을 때만 실행, 온도 `0.4` | 창작이 아니라 분석이라 온도를 낮춥니다. **실존 기업이라 환각이 특히 위험**해서 면책 문구를 결과에 남깁니다 |
| B2 | "한글 네이밍과 함께 영문 네이밍을 **동시에** 생성" | 스키마에 `name_ko`/`name_en` **두 필드** (추가 호출 아님) | 따로 호출하면 한글명과 무관한 영문명이 나옵니다. **"동시에"라는 단어가 한 번의 호출을 요구**합니다 |
| 환경 | "Python **3.10 이상**" | `dict \| None` 유니온 문법 (3.10+에서 도입) | 현재 팀 환경은 3.14.6이라 여유가 있습니다 |

### 2.2 `requirements.txt`에 무엇이 왜 들어가는가

| 패키지 | 이 요건 때문에 | 없으면 |
|--------|:-------------:|--------|
| `google-genai` | 3, 4, 5, 6 | LLM 호출 불가 |
| `openai` | 7 | 이미지 생성 불가 (제공자를 `gemini`로 두면 불필요) |
| `requests` | 7 | 이미지가 **URL로 올 때** 다운로드 불가 |
| `matplotlib` | 6 | 팔레트 PNG 생성 불가 |
| `python-dotenv` | 10 | 키를 OS 환경변수로 직접 설정해야 함 |
| `Pillow` | 7 (선택) | **없어도 됩니다** — PNG 매직 넘버 확인은 표준 라이브러리로 충분. "실제로 열리는지"까지 확인하고 싶을 때만 |

**새로 설치할 게 없는 것들** — `json` `os` `sys` `re` `base64` `datetime`은 전부 표준 라이브러리입니다.
이번 과제에서 새로 배우는 외부 라이브러리는 **matplotlib과 이미지 API SDK 두 개뿐**입니다.

### 2.3 요구사항에 없지만 우리가 추가하는 것

**요구사항에 없는 걸 넣을 때는 이유를 댈 수 있어야 합니다.** 없으면 그냥 과잉 개발입니다.

| 추가하는 것 | 요건에 있는가 | 그런데 왜 넣는가 |
|-------------|:------------:|------------------|
| **목업 모드** | ❌ | 요건 9("실패해도 계속 진행")를 **증명할 수단이 달리 없습니다.** 실제 API로는 401을 마음대로 만들 수 없습니다. 덤으로 5명 병렬 개발과 쿼터 절약도 해결됩니다 |
| **이미지 제공자 어댑터** | ❌ | 요건 7이 "DALL-E **등**"으로 열어뒀고, 이미지 API는 **유료라 팀에서 막힐 수 있습니다.** 막혔을 때 멈추지 않기 위한 보험입니다 |
| **파일 15개 분할** | ❌ | 5인 팀이라서입니다. 혼자였으면 A1-1·A1-2처럼 단일 파일로 갔습니다 |
| **`errors[]`를 결과 JSON에 저장** | ❌ (요건 9는 "출력"만 요구) | **화면 출력은 스크롤되면 사라집니다.** 나중에 결과물만 보고도 무엇이 왜 비었는지 알 수 있어야 합니다 |
| **`meta.mock` 기록** | ❌ | 목업 결과를 실제 결과로 착각하는 사고를 막습니다 |

> 반대로 **요건에 있는 것을 임의로 바꾸지 않습니다.** `brand_result.json`이라는 파일명,
> 네이밍 3~5개, 슬로건 3개, 로고 2~3장, 기본 출력 폴더 `./output` — 숫자와 이름이 명시된 것은
> 그대로 지킵니다. **자유가 있는 곳("등", "내외", "다양한 방법으로")과 없는 곳을 구분하는 것**이
> 요구사항을 읽는 첫 번째 일입니다.

---

## 3. 이 프로젝트를 관통하는 질문 4개

앞으로 내리는 모든 설계 결정은 결국 이 4개에 대한 답입니다.
**막힐 때마다 "지금 나는 몇 번 질문을 풀고 있는가"를 확인하십시오.**

```
Q1. 사람이 쓴 브리프를, 코드가 다룰 수 있는 데이터로 어떻게 바꾸는가?     → §5
Q2. AI의 자유로운 대답을, 다시 코드가 꺼내 쓸 구조로 어떻게 받는가?        → §6
Q3. 텍스트 API와 이미지 API를 어떻게 한 줄로 잇는가?                     → §7
Q4. 일부가 실패해도 어떻게 결과물을 내놓는가?                            → §8
```

그리고 팀이기 때문에 붙는 질문 하나:

```
Q5. 5명이 서로 기다리지 않고 어떻게 동시에 만드는가?                     → §9
```

---

## 4. 파이프라인 — 데이터가 아홉 번 모습을 바꾼다 ★

이 프로그램이 하는 일을 한 줄로 줄이면 **"자료형 변환의 연속"** 입니다.
브리프 파일 하나가 결과 파일이 되기까지 **모습이 아홉 번 바뀝니다.**

| # | 상태 | 자료형 | 무슨 일이 일어나는가 | 여기서 깨지면 |
|:-:|------|--------|---------------------|---------------|
| 1 | 브리프 파일 | 텍스트 파일 | 사용자가 경로를 입력 | 파일 없음 → 재입력 |
| 2 | 파싱된 브리프 | `dict` | `json.load()` | 문법 오류 → 줄·칸 안내 후 종료 |
| 3 | **검증된** 브리프 | `dict` | 필수 3필드 확인 | 필드 누락 → 종료 |
| 4 | 프롬프트 | `str` | f-string으로 값을 템플릿에 채움 | 빈칸이 들어가도 **에러가 안 남** ⚠️ |
| 5 | LLM 응답 | `str` (JSON 텍스트) | API 호출 | 인증·쿼터·타임아웃 |
| 6 | 파싱된 응답 | `dict` | `json.loads()` | 코드블록으로 감싸 옴 |
| 7 | **검증된** 응답 | `dict` | 키·타입·개수 확인 | 개수 부족 → 1회 재시도 |
| 8 | 결과의 한 칸 | `dict`의 값 | `result["naming"] = ...` | — |
| 9 | 결과 파일 | 텍스트 파일 | `json.dump()` | 폴더 권한 |

**로고는 8번에서 갈라져 나갑니다.**

```
8 결과 dict  ──→ 로고 프롬프트(str) ──→ 이미지 API ──→ bytes ──→ logo_1.png
                  (브랜드명·HEX를 재료로)              (base64 디코딩 또는 URL 다운로드)
```

**팔레트도 8번에서 갈라집니다.**

```
8 결과 dict  ──→ HEX 리스트 ──→ matplotlib 도형 ──→ savefig ──→ palette.png
```

> **이 표가 이 프로젝트의 전부입니다.** 앞으로 쓸 모든 코드는 이 아홉 칸 중 하나를
> 다음 칸으로 옮기는 코드이고, 우리가 넣을 모든 방어 코드는 **칸과 칸 사이의 화살표**에 붙습니다.

### 여기서 이미 두 가지가 보입니다

**① 검증이 두 번 나옵니다 (3번, 7번).**
둘 다 "파싱은 됐지만 쓸 수 있는지는 모른다"는 상태를 거르는 자리입니다.
**파싱 성공과 검증 성공은 다릅니다** — `{}` 도 완벽하게 유효한 JSON입니다.

**② 4번이 가장 위험합니다.**
브리프에 `keywords`가 없으면 프롬프트에 빈칸이 들어가는데, **에러가 나지 않습니다.**
그냥 엉뚱한 결과가 나옵니다. 에러가 나는 것보다 이게 더 위험합니다.
그래서 3번 검증을 반드시 4번 앞에 둡니다.

---

## 5. Q1 — 브리프를 데이터로 바꾸기

### 왜 입력을 JSON 파일로 받는가

대화형으로 업종·타겟·키워드를 하나씩 물어볼 수도 있습니다. 그런데 파일로 받습니다.

| 이유 | 설명 |
|------|------|
| **재현 가능** | 같은 브리프로 여러 번 돌려 결과를 비교할 수 있습니다. 매번 타이핑하면 조건이 달라집니다 |
| **팀 개발에 필수** | 5명이 각자 개발할 때 **같은 입력**을 써야 결과를 비교할 수 있습니다 |
| 구조가 있는 입력 | `keywords`는 리스트입니다. 대화형으로 리스트를 받으려면 구분자 규칙을 또 만들어야 합니다 |

그래서 대화형으로 받는 건 **"어느 파일을 쓸 것인가"와 "어디에 저장할 것인가"** 두 개뿐입니다.
과제 요건 1과 2가 이렇게 나뉘어 있는 이유이기도 합니다.

### 왜 필수와 선택을 나누는가

| 구분 | 필드 | 없으면 |
|------|------|--------|
| **필수** | `industry` `target` `keywords` | **프로그램이 할 일이 없습니다.** 이 셋이 없으면 프롬프트가 "업종: , 타겟: " 이 됩니다 |
| 선택 | `tone` `competitors` `notes` | 프롬프트에서 해당 줄을 빼거나 "지정 없음"으로 채우면 됩니다 |

**필수/선택의 기준은 "중요한가"가 아니라 "없으면 다음 단계를 진행할 수 있는가"입니다.**
이 기준이 §8의 Fatal/Degraded 판단과 정확히 같은 기준입니다 — 지금부터 계속 나옵니다.

---

## 6. Q2 — AI의 대답을 구조로 받기

### 문제

LLM에게 "브랜드명 4개 추천해줘"라고 하면 이렇게 옵니다.

```
좋은 브랜드명을 제안드리겠습니다!

1. 이음 - '잇다'에서 온 이름으로...
2. 리필리 - refill과 -ly의 결합으로...
```

사람은 읽을 수 있지만, **코드는 여기서 두 번째 이름만 꺼낼 수 없습니다.**
정규식으로 긁으면 형식이 조금만 바뀌어도 깨집니다.

### 우리의 대응 — 3단 방어

| 단 | 방법 | 왜 |
|:--:|------|-----|
| 1 | `response_mime_type="application/json"` + `response_schema` | **API 레벨에서 강제.** 프롬프트 지시보다 확실합니다 |
| 2 | 코드블록(` ```json `)을 벗겨내는 후처리 | 강제해도 감싸 오는 경우가 있습니다 |
| 3 | 파싱 후 **키·타입·개수를 따로 검증** | `{}` 도 유효한 JSON입니다 |

> **왜 프롬프트에 예시 JSON을 넣지 않는가** — A1-2에서 실측한 결과, 예시를 넣으면 모델이
> **예시를 먼저 출력하고 진짜 답을 이어 붙여** JSON 객체가 2개가 됐습니다(5건 중 4건 실패).
> 구조는 스키마가 강제하므로, 프롬프트에는 **조건을 산문으로만** 씁니다.

### 왜 LLM을 5번 나눠 호출하는가 ★

네이밍·슬로건·스토리·컬러·경쟁사를 **한 번에 받을 수도** 있습니다. 호출이 1번이면 쿼터도 아낍니다.
그런데 나눠서 호출합니다.

| | 1번에 몰아 받기 | **5번 나눠 호출 ← 채택** |
|---|---|---|
| 쿼터 | 1회 | 5회 |
| 슬로건 필드 하나가 깨지면 | **네이밍·스토리·컬러까지 전부 버림** | 슬로건만 `null`, 나머지는 그대로 |
| 요건 3~6 ("LLM API를 호출하여") | 해석의 여지 | 문자 그대로 충족 |
| 프롬프트 튜닝 | 하나 고치면 전체 재검증 | 담당자가 자기 것만 고침 |

**결정적인 건 두 번째 줄입니다.** 요건 9가 "실패해도 다음 단계를 계속 진행한다"인데,
한 번에 받으면 **애초에 "다음 단계"라는 게 없습니다.** 구조가 요건과 충돌합니다.

쿼터 문제는 다른 방법(목업, §9)으로 풉니다. **요건과 충돌하는 구조를 최적화로 정당화하지 않습니다.**

### 그래서 이 단계는 이렇게 구성됩니다

```
gen_naming.py  ─┐
gen_slogan.py  ─┤
gen_story.py   ─┼─→  llm.generate_json(prompt, schema, step=, errors=)  ─→ Gemini
gen_palette.py ─┤        └ 재시도·타임아웃·오류분류·키확인을 여기서만 처리
gen_competitor ─┘
```

각 `gen_*.py`가 갖는 것은 **프롬프트 문자열과 스키마 딕셔너리, 그리고 후처리** 뿐입니다.
**API를 아는 파일은 `llm.py` 하나입니다.** 그래서 모델을 바꾸거나 재시도 정책을 고칠 때
고칠 파일이 하나입니다.

---

## 7. Q3 — 두 종류의 API를 잇기

### 두 API는 성질이 다릅니다

| | LLM API | 이미지 생성 API |
|---|---|---|
| 돌려주는 것 | **텍스트** (JSON 문자열) | **바이너리** (URL 또는 base64) |
| 저장 방법 | `json.dump()` | `open(path, "wb")` |
| 실패 확인 | 파싱해보면 앎 | **파일이 생겨도 안 열릴 수 있음** |
| 검증 | 스키마 | **매직 넘버** (`\x89PNG`) |

> **"파일이 생겼는데 안 열린다"** — API가 오류를 JSON 본문으로 돌려줬는데 그걸 그대로
> `.png`로 저장하면 파일은 만들어집니다. 제출 직전에 발견하면 손 쓸 시간이 없습니다.
> 그래서 저장 전에 **첫 8바이트가 PNG 서명인지** 확인하고, 아니면 파일을 지웁니다.

### 두 API가 만나는 지점 = 로고 프롬프트

```
LLM 결과 ──────────────────────────→ 이미지 프롬프트
  naming[0].name_en  "Ieum"           "A minimalist vector logo ...
  palette.main.hex   "#1B4332"         Brand concept: circular, minimal
                                       Primary color: #1B4332
                                       flat vector, no text, no letters"
```

**LLM의 출력이 이미지 API의 입력이 됩니다.** 이게 요건 7과 과제 목표 2의 핵심입니다.

### 다만 "의존"이 아니라 "재료"입니다

여기서 한 가지 결정을 합니다. **네이밍이 실패하면 로고도 실패하는가?**

| | 하드 의존 | **소프트 의존 ← 채택** |
|---|---|---|
| 네이밍 실패 시 | 로고도 실패 (연쇄) | 브리프의 업종·키워드만으로 로고 생성 |
| 코드 | `if naming: 로고생성` | `프롬프트 += 브랜드명 if naming else ""` |

**로고 프롬프트에 브랜드명이 없어도 시안은 나옵니다.** 업종과 키워드만으로도 충분합니다.
있으면 쓰고 없으면 빼는 쪽이, 실패 하나가 두 개의 산출물을 날리는 것보다 낫습니다.

### 로고 프롬프트에서 신경 쓸 것

| 지침 | 이유 |
|------|------|
| `no text, no letters` | **이미지 모델은 글자를 뭉갭니다.** 브랜드명을 그려달라고 하면 철자가 깨집니다. 심볼만 받고 글자는 나중에 얹는 게 실무 방식이기도 합니다 |
| `solid white background` | 시안 2~3장의 배경이 제각각이면 비교가 안 됩니다 |
| 시안마다 형식 키워드를 바꿈 | 같은 프롬프트로 3장 뽑으면 비슷하게 나옵니다. `symbol mark` / `monogram` / `emblem` |

### 컬러도 같은 구조입니다

컬러 팔레트는 **LLM의 출력이 matplotlib의 입력**이 됩니다. 여기도 사이에 검증이 들어갑니다.

```
LLM ──→ "#1B4332"  ──[정규식 검증]──→ matplotlib 사각형 ──→ palette.png
        "deep green" ──[탈락]──→ 기본 팔레트로 대체 + errors 기록
```

> **스키마로 강제했는데 왜 또 검증하는가** — 스키마는 **타입이 문자열인지**만 봅니다.
> `"deep green"`도 완벽한 문자열이라 통과합니다. 그대로 matplotlib에 넘기면 거기서 예외가 나서
> **PNG가 아예 안 만들어집니다.** 정규식으로 한 번 더 걸러서, **잘못된 색만 바꾸고 나머지는 살립니다.**

여기에 그리는 과정에서 두 가지가 더 붙습니다.

| 문제 | 대응 | 왜 |
|------|------|-----|
| 한글이 `□□□`로 나옴 | 폰트 후보를 순회하며 설치된 것 지정 | matplotlib 기본 폰트에 한글 글리프가 없습니다. **팀원 5명의 OS가 다르므로 한 사람 PC에서만 되는 코드는 버그와 같습니다** |
| 어두운 칸에 검은 글씨 | 밝기 계산으로 흰/검정 자동 선택 | `0.299R + 0.587G + 0.114B` — 균등 평균이 아닌 이유는 **사람 눈이 초록에 민감하고 파랑에 둔감**하기 때문입니다 |

---

## 8. Q4 — 실패를 설계에 넣기 ★

### 판단 기준은 하나입니다

```
이 실패로 인해 "이후 단계의 입력"이 사라지는가?
   예  → 중단한다 (Fatal)
   아니오 → 기록하고 계속 진행한다 (Degraded)
```

A1-2는 체인이라 Fatal이 많았습니다. **A2-1은 단계가 독립적이라 Fatal이 브리프 관련 3건뿐**이고
나머지는 전부 계속 진행합니다. 이게 요건 9가 성립하는 구조적 근거입니다.

| Fatal (중단) | 왜 |
|---|---|
| 브리프 파일 없음 / 파싱 실패 / 필수 필드 누락 | 모든 단계의 입력이 브리프입니다 |
| 출력 폴더 생성 실패 | 저장할 곳이 없으면 **산출물이 0개**가 됩니다 |

| Degraded (계속) | 왜 |
|---|---|
| LLM 키 없음 / 잘못됨 | 로고와 저장은 여전히 가능합니다 |
| 특정 단계 호출 실패 | 그 섹션만 `null`, 나머지는 그대로 |
| HEX 형식 오류 | 그 색만 대체 |
| 이미지 API 실패 | 로고 0장, 텍스트 결과는 그대로 |

### 어떻게 "계속 진행"을 보장하는가 — 규칙이 아니라 구조로

여기가 이 프로젝트에서 가장 중요한 설계 결정입니다.

```python
# ❌ 이렇게 하면 — 호출하는 쪽이 매번 조심해야 한다
try:
    result["naming"] = gen_naming.generate(brief)
except Exception as e:
    print(e)
# ... 이걸 6번 반복. 한 번 빠뜨리면 프로그램이 죽는다.
# ... 5명이 각자 짜면 반드시 누군가는 빠뜨린다.

# ✅ 계약을 바꾼다 — 생성 함수는 예외를 밖으로 던지지 않는다
result["naming"] = gen_naming.generate(brief, errors=errors)   # 실패하면 None
result["slogans"] = gen_slogan.generate(brief, errors=errors)  # 그냥 다음 줄
```

**"실패를 예외가 아니라 반환값으로 다룬다."**
그러면 `main()`에는 `try`가 하나도 없고, **"실수로 빠뜨렸다"가 일어날 자리가 없습니다.**

5인 팀이라 이 결정의 값어치가 더 큽니다. 5명이 각자 다른 방식으로 예외를 처리하면
통합할 때 어떤 실패가 어디서 잡히는지 아무도 모르게 됩니다.
**계약을 한 줄로 못 박아두면 5명이 같은 방식으로 짭니다.**

### 대신 넓게 잡지 않습니다

```python
except Exception:   # ❌ 내 오타(NameError)까지 "API 실패"로 둔갑한다
    pass
```

요건이 "실패해도 계속 진행"이라 넓게 잡고 싶어집니다. 하지만 그러면
**오타 하나가 조용히 넘어가고 아무도 원인을 못 찾습니다.**
**계속 진행하는 것과 원인을 감추는 것은 다릅니다.** 타임아웃·연결·상태코드·파싱을 종류별로 잡습니다.

### 재시도도 성격에 따라 다릅니다

| 오류 | 재시도 | 왜 |
|------|:------:|-----|
| `503` 서버 과부하 | 3회 (2·4초) | **일시적**입니다. 잠시 뒤면 풀립니다 |
| `429` 무료 한도 소진 | **안 함** | 재시도해도 **그날은 안 풀립니다** |
| `401` 키 오류 | **안 함 + 남은 단계도 접음** | 다음 4번의 호출도 100% 같은 이유로 실패합니다. 40초 더 기다리게 할 이유가 없습니다 |
| JSON 파싱 실패 | **1회만** | 같은 프롬프트를 계속 던져도 결과가 달라지지 않습니다. A1-2의 "무한 재시도 금지" 제약을 관례로 유지 |

### 마지막 한 줄 — 저장은 무조건 실행됩니다

```python
storage.save_json(result, out_dir)      # ★ if 없음, try 없음
```

**6단계가 전부 실패해도 `brand_result.json`은 만들어집니다.**
안에는 브리프 원본과 `errors` 6건이 들어갑니다.
**"무엇이 왜 실패했는지 적힌 파일"이 파일이 없는 것보다 낫고**, 요건 8도 지켜집니다.

---

## 9. Q5 — 5명이 동시에 만들기

### 문제

프로그램은 하나인데 사람은 5명입니다. 그냥 나누면 두 가지가 터집니다.

```
① 병합 충돌   — 같은 파일을 5명이 고침
② 대기        — LLM 호출 코드가 나와야 나머지가 시작됨
```

### 대응 ① 파일 소유권

A1-1·A1-2는 단일 파일이었습니다. 이번엔 **15개 모듈로 나누고 한 사람이 한 묶음을 소유**합니다.

| 담당 | 소유 | 이 사람이 아는 것 |
|:----:|------|-------------------|
| P1 | `main.py` `brief` `storage` `logger` | 파이프라인 전체 순서 |
| P2 | `llm` `mock` `config` | **API를 아는 유일한 파일** |
| P3 | `gen_naming` `gen_slogan` `gen_story` | 프롬프트와 스키마 |
| P4 | `gen_palette` `viz_palette` | 색과 그림 |
| P5 | `image_client` `gen_logo` | 이미지 API |

이러면 충돌이 **공용 3곳**(LLM 시그니처, 결과 스키마, `main` 호출부)으로 줄고,
그 3곳만 규칙으로 관리하면 됩니다.

> 역할은 **파일 개수가 아니라 불확실성**으로 나눴습니다. P5는 파일이 2개뿐이지만
> 이번 프로젝트에서 유일하게 "해본 적 없는 API"를 맡아 실제 소요가 가장 큽니다.

### 대응 ② 인터페이스를 구현보다 먼저

```
Day 2:  llm.generate_json(prompt, schema, *, step, errors, max_retry=1) -> dict | None
        ↑ 이 한 줄만 확정하고 동결. 안이 비어 있어도 됨.

Day 3:  P2는 안을 채운다.
        P1·P3·P4는 이 줄에 대고 자기 코드를 완성한다.  ← 동시에 진행
```

**인터페이스와 구현을 분리하는 것이 병렬 작업의 전제입니다.**
이게 없으면 4명이 1명을 기다립니다.

### 대응 ③ 목업 — 이번 프로젝트에서 가장 값어치 있는 코드

`USE_MOCK=1`이면 네트워크를 타지 않고 고정 응답이 돌아옵니다.

| 목업이 푸는 문제 | 설명 |
|------------------|------|
| **대기** | 실제 LLM 구현이 없어도 나머지 4명이 자기 모듈을 완성할 수 있습니다 |
| **쿼터** | 5명 × 하루 여러 번 = 무료 한도가 오전에 끝납니다 |
| **오류 테스트** | ⭐ 실제 API로는 **401을 마음대로 만들 수 없습니다** |
| 시연 안전망 | 발표 당일 네트워크가 막혀도 돌아갑니다 |

세 번째가 특히 중요합니다.

```
USE_MOCK=fail_naming   → 네이밍만 실패, 나머지 정상       ← 요건 9를 재현 가능하게 증명
USE_MOCK=fail_image    → 로고만 실패, 텍스트·팔레트 정상
USE_MOCK=bad_hex       → HEX에 "deep green" 반환         ← 기본 팔레트 대체 확인
```

**요건 9는 "실패해도 계속 진행한다"인데, 실패를 만들 수 없으면 그걸 만족했는지 증명할 수 없습니다.**
목업의 실패 모드가 그 증명 수단입니다.

---

## 10. 그래서 최종적으로 무엇이 만들어지는가

### 실행하면 이렇게 흐릅니다

```
경로 입력 → 브리프 검증 → 요약 출력
   ↓
[1/7] 네이밍     ─┐
[2/7] 슬로건      │  서로 연결되지 않은 6개 단계
[3/7] 스토리      │  하나가 실패해도 나머지는 그대로
[4/7] 컬러        │
[5/7] 경쟁사      │
[6/7] 팔레트 PNG ─┤ ← 컬러 성공 시에만
[7/7] 로고 PNG   ─┘ ← 네이밍·컬러를 재료로 (없어도 됨)
   ↓
brand_result.json 저장  ★ 무조건
   ↓
요약: 오류 n건, 저장된 파일 목록
```

### 산출물 구성

```
output/
├─ brand_result.json     ← 텍스트 전부 + 브리프 원본 + errors + meta
├─ palette.png           ← 메인 1 + 서브 2~3, HEX·이름·사용처 표기
├─ logo_1.png
└─ logo_2.png            ← 성공한 것만 1번부터 번호
```

**`brand_result.json` 안이 어떻게 구성되는가**

| 키 | 담당 | 실패하면 | 대응 요건 |
|-----|:----:|----------|:---------:|
| `brief` | P1 | (Fatal이라 실패 시 파일 자체가 없음) | 2 |
| `naming[]` | P3 | `null` | 3, B2 |
| `slogans[]` | P3 | `null` | 4 |
| `story` | P3 | `null` | 5 |
| `palette` | P4 | **`null`이 되지 않음** — 기본 팔레트 + `is_fallback: true` | 6 |
| `competitors` | P1 | `null` (브리프에 없으면 실행 안 함) | B1 |
| `assets.palette_image` | P4 | `null` | 6 |
| `assets.logos[]` | P5 | `[]` | 7 |
| `errors[]` | 전원 | 성공해도 `[]` — **항상 존재** | 9 |
| `meta.mock` | P2 | — | — |

> **실패한 섹션을 키째 빼지 않고 `null`로 두는 이유** — 키를 빼면 읽는 쪽이 `KeyError`를
> 만납니다. **키는 있고 값이 `null`** 이면 읽는 코드가 단순해집니다.

> **`errors`가 성공해도 `[]`로 존재하는 이유** — 있는지 없는지 확인하는 코드가 필요 없어집니다.
> 결과를 읽는 쪽이 항상 같은 모양을 기대할 수 있게 만드는 것이 스키마 설계의 목적입니다.

> **`meta.mock`을 남기는 이유** — 목업 결과를 실제 결과로 착각하는 사고를 막습니다.
> 결과물이 어떻게 만들어졌는지를 결과물 자신이 기록합니다.

---

## 11. 완성하면 우리는 무엇을 알게 되는가

과제 §3이 요구하는 4가지를, **우리 프로젝트의 말로** 옮기면 이렇습니다.

| 과제가 요구하는 이해 | 우리 프로젝트에서의 답 | 근거 |
|---------------------|----------------------|------|
| **브리프 → AI 브랜드 요소 파이프라인** | 파일 → dict → 검증 → 프롬프트 → 응답 → 검증 → 결과의 한 칸 → 파일. **자료형이 아홉 번 바뀌고, 바뀌는 지점마다 방어가 붙는다.** | §4 |
| **LLM API + 이미지 API 조합** | LLM은 **텍스트**, 이미지는 **바이너리**를 돌려준다. LLM이 만든 브랜드명·HEX가 이미지 프롬프트의 재료가 되어 두 API가 이어진다. 단 재료일 뿐이라 없어도 로고는 나온다. | §7 |
| **컬러 팔레트 시각화** | HEX로만 답하게 강제 → 정규식으로 다시 검증 → matplotlib 사각형 → `savefig`. **강제와 검증은 다르다**는 것, 한글 폰트와 글자 대비가 실제 난관이라는 것. | §7 끝 |
| **API 오류 상황과 대응** | 인증·쿼터·네트워크·파싱 네 가지. 기준은 **"이후 단계의 입력이 사라지는가"** 하나. 실패를 예외가 아니라 반환값으로 다뤄 **구조로** 보장하고, 목업 실패 모드로 **증명**한다. | §8, §9 |

여기에 이번 과제로만 얻는 것이 하나 더 있습니다.

> **혼자 짜는 코드와 5명이 짜는 코드는 다르게 생겼습니다.**
> 파일을 나누고, 인터페이스를 구현보다 먼저 확정하고, 실패 처리 방식을 계약으로 못 박는 것은
> 전부 "혼자라면 하지 않았을" 결정입니다. **협업 자체가 설계에 흔적을 남깁니다.**

---

## 12. 진행하면서 계속 확인할 것

작업 중에 판단이 필요할 때 여기로 돌아오십시오.

| 상황 | 기준 |
|------|------|
| 이 실패를 중단으로 처리할까 계속으로 처리할까 | **이후 단계의 입력이 사라지는가?** (§8) |
| 재시도를 몇 번 할까 | **다시 하면 결과가 달라질 수 있는 성격인가?** 503은 예, 429·401은 아니오 (§8) |
| 검증을 또 넣어야 할까 | **파싱만으로 "쓸 수 있는 값"임이 보장되는가?** (§4, §6) |
| 이 예외를 넓게 잡을까 | **내 오타까지 삼키게 되는가?** (§8) |
| 이 필드를 필수로 할까 | **없으면 다음 단계를 진행할 수 있는가?** (§5) |
| 이 라이브러리를 왜 쓰는지 막힌다 | **요구사항의 어느 문장이 이걸 요구했는가?** (§2) |
| 요건에 없는 걸 넣어도 될까 | **요건 하나를 지키거나 증명하기 위해 필요한가?** 아니면 과잉 (§2.3) |
| 이 코드를 누가 짤까 | **누구 소유 파일에 들어가는가?** 남의 파일이면 요청 (§9) |
| 지금 실호출을 해야 할까 | **이 프롬프트를 지금 눈으로 확인해야 하는가?** 아니면 목업 (§9) |

**아직 답이 없는 것 하나** — 이미지 생성 API의 제공자·모델명·응답 형식(URL이냐 base64냐)·과금은
**Day 1에 실제로 한 번 호출해서 확인해야** 합니다. A1-2에서 목록에 있는 모델이 호출하면 404가 났던
전례가 있습니다. **문서에 적혀 있다는 것은 호출된다는 뜻이 아닙니다.**
확인 후 PRD §7.7 표를 고치는 것이 이 프로젝트의 첫 번째 실제 작업입니다.
