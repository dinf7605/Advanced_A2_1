# 📄 브랜드 아이덴티티 생성기 PRD & 7일 실행 계획 (5인 팀)

> 브랜드 브리프(JSON)를 받아 **LLM으로 네이밍·슬로건·스토리·컬러를 생성하고 →
> 이미지 생성 API로 로고 시안을 만들고 → 텍스트(JSON) + 이미지(PNG) 결과물을 저장하는**
> Python CLI 프로그램 개발 계획서

| 항목 | 내용 |
|------|------|
| 문서 버전 | v1 (2026-08-21) |
| 과제 | 2026년 AI활용학습 A2-1 |
| 진행 형태 | **5인 팀 프로젝트** |
| 작성자 | 김재민 |
| 이전 과제 | `../AI활용학습_A1_1` (Prompt Manager), `../AI활용학습_A1_2` (Trip Planner) |

---

## 0. 확정 사항 (착수 전 결정)

과제 요구사항은 제공자와 구조를 열어두었습니다. 아래는 **확정된 선택과 그 근거**입니다.
이 선택에 따라 인증 방식·응답 파싱·파일 분할이 전부 달라지므로, **Day 1 종료 시점에 고정**하고
이후에는 팀 전원 합의 없이 바꾸지 않습니다.

| 결정 항목 | 확정 | 근거 |
|-----------|------|------|
| LLM API | **Google Gemini** (`gemini-3.6-flash`) | A1-2에서 **실호출로 검증된 키·모델·SDK를 그대로 재사용**. 무료 티어로 가능하고 `response_mime_type="application/json"` + `response_schema`로 JSON 출력을 API 레벨에서 강제할 수 있어, 요건 3~6의 "구조화된 결과"를 프롬프트에만 의존하지 않아도 됨 |
| 이미지 생성 API | **어댑터 구조** — `IMAGE_PROVIDER` 환경변수로 교체 (`openai` / `gemini`) | 과제 예시는 DALL·E지만 **유료 크레딧이 필요**하고, 5명 중 누가 결제할지 정해지지 않으면 팀 전체가 멈춤. 어댑터로 빼두면 키 발급이 막혀도 **다른 제공자로 갈아끼우고 계속 진행**할 수 있음 (A1-2 평가 피드백 #8의 어댑터 구조를 계승) |
| 파일 구성 | **패키지 분할** (`main.py` + `brandkit/` 15개 모듈) | A1-1·A1-2는 단일 파일이었으나, **5명이 같은 파일을 동시에 고치면 매 병합마다 충돌**합니다. 파일 분할이 이번 과제에서 유일하게 실효성 있는 충돌 방지책입니다 (§3.2) |
| 보너스 | **둘 다 채택** — 경쟁사 분석 + 다국어 네이밍 | 5인이면 인력에 여유가 있고, 두 보너스 모두 **기존 LLM 래퍼를 재사용하는 호출 1개**라 구조 변경이 없음 |
| 목업 모드 | **채택** (`USE_MOCK=1`) | 5명이 각자 개발하면 무료 쿼터가 하루 만에 소진됩니다. 목업이 없으면 팀원 4명이 1명의 API 호출을 기다리게 됨 (§12) |
| LLM 호출 단위 | **단계별 개별 호출 5회** (한 번에 몰아 받지 않음) | 요건 3~6이 각각 "LLM API를 호출하여"라고 명시. 또한 **한 번에 받으면 하나 실패 시 전부 실패**해서 요건 9(다음 단계 계속 진행)와 정면 충돌 |

> ⚠️ **이미지 제공자 확정이 Day 1의 최우선 과제입니다.** 텍스트 API는 이미 검증됐지만
> 이미지 API는 이번에 처음 씁니다. **모델명·요청 형식·응답 형식(URL이냐 base64냐)·과금 여부**를
> 실제로 1회 호출해 확인하기 전에는 §7.7의 내용을 전부 "미검증"으로 취급하십시오.

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 이름 | Brand Kit Generator (브랜드 아이덴티티 생성기) |
| 목적 | 브랜드 브리프 JSON 하나를 입력받아 → 브랜드 요소 5종 생성 → 로고·팔레트 이미지까지 자동 생성하는 CLI 프로그램 |
| 환경 | Python 3.10+ / 외부 라이브러리 사용 (`google-genai`, `openai`, `requests`, `python-dotenv`, `matplotlib`) |
| 학습 목표 | 브리프→AI 생성 파이프라인, LLM + 이미지 API 조합, 컬러 팔레트 시각화, API 오류 대응 |
| 실행 형태 | 터미널 CLI, **대화형 입력** (`print` / `input`) |
| 산출물 | `output/brand_result.json`, `output/palette.png`, `output/logo_1~3.png`, `README.md` |
| 인원 | 5명 (§3) |

### A1-2와 달라지는 점

| 항목 | A1-2 (Trip Planner) | A2-1 (Brand Kit) |
|------|---------------------|------------------|
| 인원 | 1인 | **5인 팀** — 인터페이스 계약·파일 소유권·병합 규칙이 새로 필요 |
| 입력 방식 | CLI 인자 1회 (`argparse`) | **대화형** (`input()`) — 요건 1에 명시 |
| API 종류 | LLM + 지도(텍스트) | **LLM + 이미지 생성** — 응답이 바이너리라 저장 방식이 다름 |
| 산출물 | Markdown 리포트 1개 | **JSON 1개 + PNG 3~4개** — 이미지 파일 무결성까지 확인해야 함 |
| 단계 관계 | **직렬 체인** (도시 → 맛집 → 리포트) | **대부분 병렬·독립** (네이밍/슬로건/스토리/컬러가 서로를 필요로 하지 않음) |
| 실패의 성격 | 앞 단계 실패 = 뒤 단계 불가 | **한 단계 실패해도 나머지는 그대로 나옴** — 요건 9가 이 구조를 전제로 함 |
| 새 라이브러리 | — | **matplotlib** (팔레트 시각화), 이미지 API SDK |

> A1-2의 핵심 난이도가 "체인이 끊기지 않게 하기"였다면, **A2-1의 핵심 난이도는
> "5명이 만든 6개 조각이 하나의 JSON으로 합쳐지게 하기"** 입니다.
> 그래서 이 PRD는 §3(팀 규칙)과 §4.3(인터페이스 계약)에 가장 많은 지면을 씁니다.

### 실제 환경 확인 결과 (2026-08-21 측정)

| 항목 | 결과 | 조치 |
|------|------|------|
| Python | **3.14.6** | 요건(3.10+) 충족 |
| `requests` | 설치됨 | — |
| `python-dotenv` | 설치됨 | — |
| `google-genai` | 설치됨 (A1-2에서 사용) | 그대로 재사용 |
| `matplotlib` | ✅ **3.11.1 설치 완료** | 한글 폰트 `Malgun Gothic` 사용 가능 확인 |
| `openai` | **미설치** | Day 1, 제공자 확정 후 설치 |
| `Pillow` | **미설치 (선택)** | PNG 매직넘버 검증은 표준 라이브러리로 충분. JPEG/WebP 변환이 필요할 때만 설치 |
| 콘솔 인코딩 | **cp949 — 한글 깨짐 재현됨** | `sys.stdout.reconfigure(encoding="utf-8")` 필수 (A1-2와 동일) |

---

## 2. 요구사항 추적표 (과제 요건 ↔ 대응 계획)

### 2.1 산출물

| 과제 요구 | 파일 | 담당 | 절 |
|-----------|------|:----:|----|
| 텍스트 결과 JSON | `output/brand_result.json` | P1 | §9.1 |
| 컬러 팔레트 시각화 PNG | `output/palette.png` | P4 | §8 |
| 로고 시안 PNG 2~3개 | `output/logo_1.png` ~ `logo_3.png` | P5 | §7.7 |
| 실행 가능한 CLI 프로그램 | `main.py` + `brandkit/` | 전원 | §4.2 |

### 2.2 기능 요구사항

| # | 과제 요건 | 대응 | 담당 | 절 |
|:-:|-----------|------|:----:|----|
| 1 | 대화형 입력(`print`/`input`), 필수=브리프 경로, 선택=출력 폴더(기본 `./output`) | `main.py`의 `ask_inputs()` | P1 | §5 |
| 2 | 브리프 JSON — 필수 `industry`/`target`/`keywords`, 선택 `tone`/`competitors`/`notes` | `brief.py`의 로드 + 검증 | P1 | §6 |
| 3 | 네이밍 3~5개 + 각 의미 | `gen_naming.py` | P3 | §7.2 |
| 4 | 슬로건 3개, 톤앤매너 반영 | `gen_slogan.py` | P3 | §7.3 |
| 5 | 브랜드 스토리 300자 내외 (배경·철학·비전) | `gen_story.py` | P3 | §7.4 |
| 6 | 컬러 메인 1 + 서브 2~3, HEX, matplotlib 시각화 | `gen_palette.py` + `viz_palette.py` | P4 | §7.5, §8 |
| 7 | 이미지 생성 API로 로고 2~3개 PNG 저장 | `image_client.py` + `gen_logo.py` | P5 | §7.7 |
| 8 | 출력 폴더에 저장, 텍스트는 `brand_result.json` | `storage.py` | P1 | §9 |
| 9 | **API 실패 시 메시지 출력 후 다음 단계 계속** | 래퍼가 예외를 밖으로 던지지 않는 계약 | P2 | §4.3, §10 |
| 10 | 키를 코드에 쓰지 않음, 환경변수/설정파일 | `.env` + `python-dotenv` | P2 | §11 |

### 2.3 보너스

| # | 보너스 | 대응 | 담당 | 절 |
|:-:|--------|------|:----:|----|
| B1 | 경쟁사 분석 → 차별화 포인트 제안 | `gen_competitor.py`, 브리프에 `competitors`가 있을 때만 실행 | P1 | §7.6 |
| B2 | 다국어 네이밍 (한글 + 영문 동시) | 네이밍 스키마에 `name_ko` / `name_en` 두 필드 | P3 | §7.2 |

> **B2를 "추가 호출"이 아니라 "스키마 확장"으로 처리하는 이유** — 영문 네이밍을 따로
> 호출하면 한글명과 무관한 이름이 나와서 "같은 브랜드의 두 표기"가 되지 않습니다.
> 한 번의 호출에서 쌍으로 받아야 `이음(Ieum) / Weaver` 처럼 의미가 이어집니다.

---

## 3. 팀 구성 & 역할 (5인) ★

### 3.1 역할 표

**원칙: 한 사람이 파일을 소유한다. 남의 파일은 고치지 않고, 필요하면 소유자에게 요청한다.**

| # | 역할 | 소유 파일 | 핵심 책임 | 최대 리스크 |
|:-:|------|-----------|-----------|-------------|
| **P1** | 팀장 · 통합/CLI | `main.py`, `brandkit/brief.py`, `storage.py`, `logger.py`, `gen_competitor.py` | 대화형 입력, 브리프 검증, 파이프라인 순서, 진행 로그, 결과 저장, **브랜치 병합·릴리스**, README, 경쟁사 분석(B1) | 통합 작업이 마지막 날로 몰림 |
| **P2** | LLM 코어 | `brandkit/llm.py`, `mock.py`, `config.py` | LLM 호출 래퍼 **1개로 통일**, JSON 강제·스키마 검증·재시도·오류 분류, 목업 엔진, 키 로딩 | **Day 2까지 인터페이스를 못 내면 P1·P3·P4가 전부 대기** |
| **P3** | 브랜드 텍스트 | `brandkit/gen_naming.py`, `gen_slogan.py`, `gen_story.py` | 프롬프트 설계·스키마 정의·후처리(글자수/개수 보정), 다국어 네이밍(B2) | 프롬프트 반복 실험으로 쿼터 소모 |
| **P4** | 컬러 · 시각화 | `brandkit/gen_palette.py`, `viz_palette.py` | 컬러 추천 프롬프트, **HEX 검증**, matplotlib 팔레트 PNG, 한글 폰트·글자 대비 | 한글 폰트 깨짐(□□□), 환경별 폰트 차이 |
| **P5** | 로고 이미지 | `brandkit/image_client.py`, `gen_logo.py` | 이미지 API 어댑터, 로고 프롬프트 설계, PNG 저장·검증, 실패 폴백 | **키 발급·과금이 프로젝트 전체 최대 리스크** |

**균형에 대한 메모** — 사람마다 파일 수가 다릅니다. 기준은 파일 개수가 아니라 **불확실성의 양**입니다.
P5는 파일 2개지만 이번 프로젝트에서 유일하게 "해본 적 없는 API"를 맡고 있어 실제 소요가 가장 큽니다.
P1은 앞(Day 1~2 CLI)과 뒤(Day 6~7 통합)에 일이 몰리므로 중간에 여유가 생겨 보너스 B1을 받습니다.

### 3.2 왜 파일을 나누는가 (A1-1·A1-2와 다른 점)

| 방식 | 5인 팀에서의 문제 |
|------|-------------------|
| 단일 파일 `brand_kit.py` (A1-1·A1-2 방식) | 5명이 같은 파일을 고침 → **병합 충돌이 매번 발생**하고, 충돌 해결 중에 남의 코드를 지우는 사고가 남 |
| 기능별 파일 분할 **← 채택** | 각자 자기 파일만 수정 → 충돌 지점이 **공용 파일 3개(`llm.py` 시그니처, 스키마 정의, `main.py` 호출부)로 축소**. 이 3곳만 규칙으로 관리하면 됨 |
| 사람당 별도 저장소 | 통합 자체가 불가능 |

> **파일 소유권은 코드 품질 규칙이 아니라 병합 사고 방지 규칙입니다.**
> "이 줄 하나만 고치면 되는데"가 5명분 모이면 충돌 5건이 됩니다.

### 3.3 협업 규칙 (3가지)

**규칙 1 — 인터페이스 동결 (Day 2 종료 시점)**

`llm.py`의 함수 시그니처와 `brand_result.json` 스키마는 **Day 2 저녁에 동결**합니다.
동결 이후 변경은 팀 전원 합의 + PRD 수정 + 전체 공지가 있어야 합니다.
동결이 없으면 P3가 만든 코드가 P2의 리팩터링으로 하루아침에 깨집니다.

**규칙 2 — 목업 우선 개발 (§12)**

인터페이스가 동결되면 **API 키 없이도 전체 파이프라인이 돌아야 합니다.**
P3는 P2의 실제 LLM 구현을 기다리지 않고, 목업이 돌려주는 고정 JSON으로 자기 모듈을 완성합니다.

**규칙 3 — 실패는 예외가 아니라 반환값 (§4.3)**

모든 생성 함수는 **예외를 밖으로 던지지 않습니다.** 실패하면 `None`(또는 빈 리스트)을 돌려주고
`errors` 리스트에 기록합니다. 이 계약 하나가 요건 9("실패해도 다음 단계 계속 진행")를
개인의 주의력이 아니라 **구조로** 보장합니다.

### 3.4 Git 전략

```
main            ← 제출본만. P1만 병합.
 └ develop      ← 통합 브랜치. 매일 저녁 여기로 모임.
    ├ feat/p1-cli
    ├ feat/p2-llm-core
    ├ feat/p3-text
    ├ feat/p4-palette
    └ feat/p5-logo
```

| 규칙 | 내용 |
|------|------|
| 브랜치명 | `feat/p{번호}-{기능}` |
| 커밋 메시지 | `type: 한국어 설명` (`feat`/`fix`/`docs`/`chore`/`refactor`/`style`/`merge`) — A1-1부터의 관례 |
| 병합 | `--no-ff` (fast-forward로 붙으면 브랜치 기록이 그래프에서 사라짐) |
| 병합 권한 | `develop` ← 각자 PR, **리뷰어 1명 승인 후** 본인이 병합 / `main` ← P1만 |
| 병합 전 필수 | `USE_MOCK=1 python main.py` 가 끝까지 도는지 확인 |
| 금지 | `.env` 커밋, 남의 소유 파일 수정, `main` 직접 푸시 |

### 3.5 데일리 운영

| 시각 | 활동 | 소요 |
|------|------|------|
| 매일 시작 | 스탠드업 — ①어제 한 것 ②오늘 할 것 ③막힌 것 (1인 2분) | 10분 |
| 매일 저녁 | `develop` 통합 + `USE_MOCK=1` 전체 실행 확인 (P1 주관) | 20분 |
| Day 4 / Day 6 | **실키 통합 테스트** — 이날만 실제 API를 전 단계 호출 | 40분 |

> **"막힌 것"을 말하지 않고 혼자 하루를 보내는 것이 팀 프로젝트 최대의 손실입니다.**
> 30분 이상 막힌 문제는 무조건 스탠드업에서 공유하고, 담당자를 붙여 페어로 해결합니다.

### 3.6 공용 API 키 운영

| 항목 | 방침 |
|------|------|
| 키 발급 | **각자 자기 계정으로 발급** (Gemini는 무료 티어라 5명 각자 가능) |
| 키 공유 | **하지 않음.** 공유하면 한 사람의 실수로 전원의 쿼터가 소진되고, 유출 시 책임 소재가 사라짐 |
| 이미지 API | 유료라 각자 발급이 어려우면 **P5의 키 1개만 사용**하고 나머지는 목업으로 개발. 실키 테스트는 통합일에 P5 PC에서 |
| 쿼터 절약 | 개발 중 기본값은 `USE_MOCK=1`. 실호출은 "지금 이 프롬프트를 확인해야 할 때"만 |
| 유출 시 | 파일 수정이 아니라 **즉시 재발급**. 팀 채널에 공지 |

---

## 4. 시스템 구조

### 4.1 처리 흐름

```
[시작]
   │
   ├─ 1. 대화형 입력 ─────────── 브리프 경로(필수), 출력 폴더(선택, 기본 ./output)
   │        │
   │        └─ ❌ 파일 없음 / JSON 오류 / 필수 필드 누락 → 안내 후 종료 (Fatal, E1~E3)
   │
   ├─ 2. 환경 점검 ──────────── LLM 키 / 이미지 키 존재 확인 → 없으면 경고 후 계속 (E4, E5)
   │
   ├─ 3. 생성 단계 (아래 6개는 서로 독립 — 하나 실패해도 나머지 진행)
   │     ├─ 3-1 네이밍      → naming[]      (P3)
   │     ├─ 3-2 슬로건      → slogans[]     (P3)
   │     ├─ 3-3 스토리      → story         (P3)
   │     ├─ 3-4 컬러 팔레트  → palette       (P4)
   │     ├─ 3-5 경쟁사 분석  → competitors   (P1, 브리프에 competitors 있을 때만)
   │     └─ 3-6 로고 시안    → logo_*.png    (P5)  ← 소프트 의존
   │
   ├─ 4. 팔레트 시각화 ──────── palette.png  (P4)  ← 항상 시도 (실패 시 기본 팔레트)
   │
   └─ 5. 저장 ──────────────── brand_result.json  ★ 무조건 실행
[끝]
```

**의존 관계 3종**

| 종류 | 예 | 처리 |
|------|-----|------|
| **하드 의존** | 브리프 → 모든 단계 | 브리프가 없으면 프로그램이 할 일이 없음 → **Fatal (즉시 종료)** |
| **하드 의존** | `palette` JSON → `palette.png` | 색이 없으면 그릴 수 없음 → 팔레트 실패 시 **PNG 생략 + errors 기록** |
| **소프트 의존** | 네이밍·팔레트 → 로고 프롬프트 | 있으면 브랜드명·HEX를 프롬프트에 넣고, **없으면 브리프만으로 로고 생성** |

> **로고를 소프트 의존으로 만든 이유** — 하드 의존으로 두면 네이밍 호출 1회가 실패했을 때
> 로고까지 연쇄로 실패합니다. 로고 프롬프트에 브랜드명이 없어도 "업종 + 키워드 + 톤"만으로
> 충분히 시안을 만들 수 있으므로, 있으면 쓰고 없으면 빼는 방식이 요건 9에 부합합니다.

**저장이 마지막이면서 무조건 실행되는 이유** — 6단계가 전부 실패해도 `brand_result.json`은
생성됩니다. 안에는 브리프 원본과 `errors` 6건이 들어갑니다. **"무엇이 왜 실패했는지 적힌 파일"이
파일이 아예 없는 것보다 낫고**, 요건 8("출력 폴더에 결과물을 저장한다")도 지켜집니다.

### 4.2 파일 구조

```
AI활용학습_A2_1/
├─ main.py                      # P1 진입점 — 대화형 입력 + 파이프라인 오케스트레이션
├─ brandkit/
│  ├─ __init__.py
│  ├─ config.py                 # P2 키·설정 로딩 (.env)
│  ├─ llm.py                    # P2 ★공용★ LLM 호출 래퍼 (JSON 강제·재시도·오류분류)
│  ├─ mock.py                   # P2 목업 응답
│  ├─ brief.py                  # P1 브리프 로드 + 검증
│  ├─ storage.py                # P1 출력 폴더 준비, brand_result.json 저장
│  ├─ logger.py                 # P1 진행 로그 + errors 수집기
│  ├─ gen_naming.py             # P3 네이밍 (+ 다국어 B2)
│  ├─ gen_slogan.py             # P3 슬로건
│  ├─ gen_story.py              # P3 스토리
│  ├─ gen_competitor.py         # P1 경쟁사 분석 (B1)
│  ├─ gen_palette.py            # P4 컬러 추천 + HEX 검증
│  ├─ viz_palette.py            # P4 matplotlib 팔레트 PNG
│  ├─ image_client.py           # P5 ★어댑터★ 이미지 생성 API
│  └─ gen_logo.py               # P5 로고 프롬프트 + PNG 저장
├─ briefs/
│  └─ sample_brief.json         # 부록 B
├─ output/                      # 결과물 (제출물이므로 커밋함)
├─ docs/screenshots/            # 스크린샷 NN_이름.png
├─ .env                         # ❌ 커밋 금지
├─ .env.example                 # ✅ 키 "이름"만 (부록 C)
├─ .gitignore
├─ requirements.txt
├─ README.md
└─ brand_kit_PRD.md             # 이 문서
```

### 4.3 공용 인터페이스 계약 (Day 2 동결 대상) ★

**이 절이 5명을 병렬로 일하게 하는 핵심입니다.** 아래 시그니처가 고정되면
나머지는 각자 독립적으로 만들 수 있습니다.

```python
# ── brandkit/llm.py  (P2 소유) ──────────────────────────────────────
def generate_json(
    prompt: str,
    schema: dict,          # Gemini response_schema 형식
    *,
    step: str,             # "naming" | "slogan" | ... — 로그와 errors에 그대로 들어감
    errors: list,          # 실패를 append 할 리스트 (가변 객체라 return 불필요)
    max_retry: int = 1,    # 파싱/검증 실패 시 재시도 횟수. 1을 넘기지 않음
    brief: dict | None = None,   # 목업이 읽기 좋은 응답을 만들 때만 사용 (실호출에선 무시)
) -> dict | None:
    """LLM에 JSON 응답을 요청한다.
    성공: 검증을 통과한 dict.  실패: None (+ errors에 기록).
    ★ 어떤 경우에도 예외를 호출자에게 던지지 않는다. ★"""


# ── brandkit/image_client.py  (P5 소유) ─────────────────────────────
def generate_images(
    prompt: str,
    n: int,                # 2~3
    *,
    step: str,
    errors: list,
) -> list[bytes]:
    """이미지 생성 API를 호출해 PNG 바이트 리스트를 돌려준다.
    실패: 빈 리스트 (+ errors에 기록).  ★예외를 던지지 않는다.★
    부분 성공 허용 — 3장 요청에 2장만 오면 2장을 돌려준다."""


# ── brandkit/logger.py  (P1 소유) ───────────────────────────────────
def step_start(label: str) -> None: ...            # "[3/7] 슬로건 생성 중..."
def step_ok(label: str, detail: str = "") -> None: ...   # "✅ 3개 생성"
def step_fail(label: str, reason: str) -> None: ...      # "❌ 실패 — 인증 오류(401)"
def step_skip(label: str, reason: str) -> None: ...      # "⚠️  건너뜀 — ..."
def print_summary(result: dict, out_dir: str, errors: list) -> None: ...  # 마지막 요약 블록

def record_error(errors: list, *, step: str, type_: str, message: str) -> None:
    """errors에 표준 형식으로 한 건 추가한다 (§9.4).
    message에 예외 원문을 통째로 넣지 않는다 — 요청 URL·헤더가 딸려와 파일에 남는다."""


# ── 각 생성 모듈이 지켜야 할 공통 형태 (P1·P3·P4·P5) ───────────────
def generate(brief: dict, *, errors: list):
    """브리프를 받아 자기 섹션을 만든다. 실패하면 None. 예외를 던지지 않는다."""
```

> **`errors`를 인자로 받아 `append` 하는 이유** — 리스트는 가변 객체라 함수 안에서 `append` 하면
> 호출한 쪽 리스트에 그대로 반영됩니다. 그래서 `return errors`가 필요 없고,
> **모든 모듈이 같은 오류 목록 하나를 공유**하게 됩니다. (A1-2에서 검증된 방식)

> **`step` 문자열을 인자로 받는 이유** — 실패했을 때 "어느 단계에서" 실패했는지가
> `brand_result.json`에 남아야 합니다. 함수 이름으로 추측하지 않고 호출자가 명시합니다.

---

## 5. CLI 명세 (대화형)

### 5.1 입력 항목 (요건 1)

| 순서 | 프롬프트 | 필수 | 기본값 | 검증 |
|:----:|----------|:----:|--------|------|
| 1 | `브랜드 브리프 JSON 파일 경로를 입력하세요: ` | ✅ | 없음 | 존재 여부 + 확장자 + JSON 파싱 (§6.2) |
| 2 | `결과를 저장할 폴더 (Enter=./output): ` | ❌ | `./output` | 없으면 생성, 쓰기 권한 확인 |
| 3 | `로고 시안 개수 (2~3, Enter=2): ` | ❌ | `2` | 정수 2~3 이외는 재입력 |

```python
def ask_inputs() -> tuple[str, str, int]:
    print("=" * 56)
    print("  브랜드 아이덴티티 생성기 (Brand Kit Generator)")
    print("=" * 56)

    while True:                                    # 필수 입력은 통과할 때까지 반복
        path = input("\n브랜드 브리프 JSON 파일 경로를 입력하세요: ").strip().strip('"')
        if not path:
            print("  ⚠️  경로는 반드시 입력해야 합니다.")
            continue
        if not os.path.isfile(path):
            print(f"  ❌ 파일을 찾을 수 없습니다: {path}")
            print("     예시: briefs/sample_brief.json")
            continue
        break

    out = input("결과를 저장할 폴더 (Enter=./output): ").strip() or "./output"
    ...
```

> **`.strip('"')`를 넣는 이유** — Windows 탐색기에서 "경로로 복사"를 하면 `"C:\...\a.json"` 처럼
> 큰따옴표가 붙어서 복사됩니다. 이걸 그대로 `os.path.isfile`에 넣으면 무조건 실패합니다.
> 실제로 가장 많이 나오는 사용자 오류이므로 코드에서 흡수합니다.

> **필수 입력은 `while True` 루프, 선택 입력은 1회만** — 필수는 값이 없으면 진행이 불가능하지만,
> 선택은 빈 값이 곧 "기본값 사용"이라는 유효한 답이기 때문입니다.

### 5.2 화면 설계 — 진행 로그

```
============================================================
  브랜드 아이덴티티 생성기 (Brand Kit Generator)
============================================================

브랜드 브리프 JSON 파일 경로를 입력하세요: briefs/sample_brief.json
결과를 저장할 폴더 (Enter=./output): 
로고 시안 개수 (2~3, Enter=2): 

✅ 브리프 로드 완료
   업종   : 친환경 리필 생활용품
   타겟   : 20~30대 1인 가구
   키워드 : 지속가능, 미니멀, 순환
   톤앤매너: 따뜻하고 담백한

[1/7] 브랜드 네이밍 생성 중...           ✅ 4개 생성
[2/7] 슬로건 생성 중...                  ✅ 3개 생성
[3/7] 브랜드 스토리 생성 중...           ✅ 312자
[4/7] 컬러 팔레트 생성 중...             ✅ 메인 1 + 서브 3
[5/7] 경쟁사 분석 중...                  ⚠️  건너뜀 (브리프에 competitors 없음)
[6/7] 팔레트 이미지 저장 중...           ✅ output/palette.png
[7/7] 로고 시안 생성 중... (2장)         ❌ 실패 — 이미지 API 인증 오류(401)
      └ 키를 확인하세요: .env 의 OPENAI_API_KEY

------------------------------------------------------------
⚠️  1건의 오류가 있었지만 나머지 결과는 저장되었습니다.
------------------------------------------------------------
📁 output/brand_result.json
📁 output/palette.png
소요 시간: 21.4초
```

| 설계 의도 | 내용 |
|-----------|------|
| `[n/7]` 진행 표시 | LLM 호출은 각 3~10초라 아무 출력이 없으면 **멈춘 것처럼 보입니다**. 호출 **전에** 라벨을 먼저 찍습니다 (A1-2에서 확인된 문제) |
| ✅ / ⚠️ / ❌ 3단계 | 성공 / 건너뜀 / 실패를 눈으로 구분. 요건 9의 "에러 메시지 출력"이 여기서 충족됩니다 |
| 실패 시 다음 줄에 조치 안내 | "401" 만으로는 사용자가 뭘 해야 할지 모릅니다. **원인 + 다음 행동**을 한 줄로 |
| 마지막 요약 | 오류 건수와 저장된 파일 목록. **부분 실패해도 결과가 있다는 것을 명시** |

> ⚠️ 진행 로그에 **요청 URL·헤더·키를 절대 출력하지 마십시오.** 스크린샷을 제출물에 넣는
> 순간 키가 그대로 공개됩니다. 가장 흔한 유출 경로입니다 (§11.4).

---

## 6. 브리프 명세

### 6.1 스키마

| 필드 | 타입 | 필수 | 설명 | 예 |
|------|------|:----:|------|-----|
| `industry` | str | ✅ | 업종 | `"친환경 리필 생활용품"` |
| `target` | str | ✅ | 타겟 고객 | `"20~30대 1인 가구"` |
| `keywords` | list[str] | ✅ | 핵심 키워드 (1개 이상) | `["지속가능", "미니멀", "순환"]` |
| `tone` | str | ❌ | 톤앤매너 | `"따뜻하고 담백한"` |
| `competitors` | list[str] | ❌ | 경쟁사 (있으면 B1 실행) | `["알맹상점", "지구샵"]` |
| `notes` | str | ❌ | 추가 요청사항 | `"영문 표기도 함께 제안"` |

### 6.2 검증 규칙

**파싱에 성공했다고 검증에 성공한 것이 아닙니다.** `json.load`가 통과해도 아래를 따로 봅니다.

| 순서 | 검사 | 실패 시 |
|:----:|------|---------|
| 1 | 파일 존재 | E1 — 경로 재입력 (§5.1 루프) |
| 2 | `json.load` 성공 | E2 — **줄·칸 번호까지 안내** 후 종료 |
| 3 | 최상위가 `dict` | E3 — 종료 |
| 4 | 필수 3개 키 존재 | E3 — **없는 키 이름을 모두 나열**하고 종료 |
| 5 | `industry`/`target`이 비지 않은 문자열 | E3 — 종료 |
| 6 | `keywords`가 비지 않은 리스트 | E3 — 종료 |
| 7 | 선택 필드 타입 불일치 | ⚠️ 경고만 출력하고 **해당 필드를 버린 뒤 계속** |

```python
# 4번 — 없는 키를 하나씩 알려주지 말고 한 번에 모아서 보여줍니다.
missing = [k for k in ("industry", "target", "keywords") if k not in data]
if missing:
    print(f"❌ 브리프에 필수 항목이 없습니다: {', '.join(missing)}")
    print("   필수: industry(업종), target(타겟), keywords(키워드 리스트)")
    sys.exit(1)

# 2번 — JSONDecodeError는 위치 정보를 갖고 있습니다. 버리지 말고 보여줍니다.
except json.JSONDecodeError as e:
    print(f"❌ JSON 형식 오류 — {e.lineno}번째 줄 {e.colno}번째 칸: {e.msg}")
    print("   쉼표 누락이나 따옴표 짝이 맞는지 확인하세요.")
```

> **7번만 경고인 이유** — 선택 필드는 없어도 프로그램이 돌아갑니다. `tone`이 리스트로 잘못
> 들어왔다고 전체를 중단시키면 요건 9의 정신과 어긋납니다. **없는 것으로 간주하고 진행**합니다.

### 6.3 예시 파일

부록 B의 `briefs/sample_brief.json` 참조.

---

## 7. 외부 API 명세

### 7.0 REST API 기초 (과제 목표 대응)

| 개념 | 이번 프로젝트에서의 위치 |
|------|--------------------------|
| **엔드포인트** | 요청을 받는 주소. LLM·이미지가 서로 다른 주소를 씀 |
| **인증** | 헤더에 키를 실어 보냄 (`Authorization: Bearer ...`). **URL 쿼리에 넣지 않습니다** — 로그·브라우저 기록에 남음 |
| **GET vs POST** | 조회는 GET(파라미터가 URL에 붙음), **생성은 POST**(데이터를 본문 JSON에 담음). 이번 프로젝트는 **전부 POST** |
| **상태 코드** | `200` 성공 / `401`·`403` 인증 / `429` 쿼터·속도 제한 / `5xx` 서버 |
| **응답 본문** | LLM은 JSON 텍스트, **이미지는 URL 또는 base64 문자열** — 둘 다 PNG 바이트로 바꿔야 저장됨 (§7.7) |

> **A1-2와의 차이** — A1-2는 지도 API가 GET이라 GET/POST를 둘 다 다뤘습니다.
> 이번엔 전부 POST지만, 대신 **응답이 텍스트가 아니라 바이너리인 경우**를 처음 다룹니다.

### 7.1 LLM 공통 래퍼 (`llm.py` — P2)

**핵심 결정: LLM을 호출하는 코드는 이 파일 하나뿐입니다.** P3·P4·P1은 프롬프트와 스키마만
만들어 넘기고, 재시도·타임아웃·오류 분류·키 확인은 전부 여기서 처리합니다.

| 항목 | 값 | 근거 |
|------|-----|------|
| 모델 | `gemini-3.6-flash` | A1-2에서 실호출 검증됨 |
| JSON 강제 | `response_mime_type="application/json"` + `response_schema` | 프롬프트 지시만으로는 코드블록(` ```json `)으로 감싸 오는 경우가 있음 |
| 타임아웃 | 60초 | 없으면 무한 대기처럼 보임 (A1-2에서 실제 발생) |
| 파싱/검증 실패 재시도 | **최대 1회** | 같은 프롬프트를 다시 던져도 결과가 크게 달라지지 않음. A1-2의 "무한 재시도 금지" 제약을 관례로 유지 (A2-1 제약사항에는 명시 없음) |
| `503` 재시도 | 최대 3회, 2·4초 백오프 | 일시적 과부하 — 성격이 다름 (§10 E7-a) |
| `429` 재시도 | **하지 않음** | 무료 한도 소진은 재시도해도 그날 안 풀림 |
| 온도 | 네이밍·슬로건 `0.9` / 스토리·컬러 `0.7` / 경쟁사 `0.4` | 창의성이 필요한 단계는 높게, 사실 기반 분석은 낮게 |

**JSON 파싱 실패에 대한 방어 3단**

```python
# 1단 — API 레벨에서 강제 (가장 확실)
config = {"response_mime_type": "application/json", "response_schema": schema}

# 2단 — 그래도 코드블록으로 감싸 오면 벗겨낸다
text = text.strip()
if text.startswith("```"):
    text = text.split("```")[1].removeprefix("json").strip()

# 3단 — 파싱 성공 ≠ 검증 성공. 키·타입·개수를 따로 본다.
data = json.loads(text)
if not validate(data, schema):      # 실패하면 파싱 실패와 동일하게 1회 재시도
    ...
```

> **"파싱 성공 ≠ 검증 성공"** — `json.loads`는 `{}` 도 통과시킵니다. 네이밍을 4개 요청했는데
> 0개가 온 응답도 문법적으로는 완벽한 JSON입니다. **개수·키·타입을 반드시 따로 확인**하십시오.
> (A1-2에서 확인된 사항)

> ⚠️ **1차 프롬프트에 예시 JSON 블록을 넣지 마십시오.** A1-2 실측에서 예시를 넣으면 모델이
> 예시를 먼저 출력하고 진짜 답을 이어 붙여 **JSON 객체가 2개**가 됐습니다(5건 중 4건 실패).
> 구조는 `response_schema`가 강제하므로 프롬프트에는 산문으로만 지시합니다.

### 7.2 네이밍 (`gen_naming.py` — P3, 요건 3 + 보너스 B2)

**프롬프트 골격**

```
당신은 브랜드 네이밍 전문가입니다.
아래 브리프에 맞는 브랜드명 후보를 4개 제안하세요.

업종: {industry}
타겟: {target}
키워드: {keywords를 쉼표로}
톤앤매너: {tone 또는 "지정 없음"}
추가 요청: {notes 또는 "없음"}

조건:
- 한글 이름과 그에 대응하는 영문 표기를 함께 제안합니다.
- 영문 표기는 한글 이름의 음차 또는 같은 의미의 영어 단어여야 합니다.
- 각 이름에 대해 어원·의미를 한 문장으로 설명합니다.
- 이름은 2~5음절로 부르기 쉬워야 하며, 서로 뚜렷하게 달라야 합니다.
- 실존하는 유명 브랜드명과 같은 이름은 제안하지 않습니다.
```

**응답 스키마**

```python
NAMING_SCHEMA = {
  "type": "object",
  "properties": {
    "naming": {
      "type": "array", "minItems": 3, "maxItems": 5,
      "items": {
        "type": "object",
        "properties": {
          "name_ko":   {"type": "string"},   # 한글명
          "name_en":   {"type": "string"},   # 영문명 (보너스 B2)
          "meaning":   {"type": "string"},   # 의미·유래 (요건 3)
          "rationale": {"type": "string"},   # 브리프와의 연결
        },
        "required": ["name_ko", "name_en", "meaning"],
      },
    }
  },
  "required": ["naming"],
}
```

**후처리 (P3 책임)**

| 검사 | 처리 |
|------|------|
| 개수가 3 미만 | `errors`에 `type:"invalid"` 기록 후 **1회 재시도** |
| 개수가 5 초과 | 앞에서 5개만 취함 (재시도하지 않음 — 요건은 충족됨) |
| `name_ko` 중복 | 중복 제거 후 개수 재확인 |
| `meaning`이 빈 문자열 | 해당 항목 제거 후 개수 재확인 |

> **개수 부족은 재시도하고 초과는 자르는 이유** — 요건은 "3~5개"입니다. 6개가 왔을 때
> 5개로 자르면 요건을 만족하지만, 2개가 왔을 때는 어떤 후처리로도 3개를 만들 수 없습니다.
> **후처리로 고칠 수 있으면 고치고, 없으면 재시도**가 원칙입니다.

### 7.3 슬로건 (`gen_slogan.py` — P3, 요건 4)

```
아래 브랜드에 어울리는 슬로건(태그라인) 3개를 제안하세요.
브랜드명 후보: {네이밍 결과가 있으면 나열, 없으면 이 줄을 넣지 않음}
...
조건:
- 각 슬로건은 20자 이내로 짧고 기억하기 쉬워야 합니다.
- 톤앤매너 "{tone}"이 문장의 어미와 어휘에서 드러나야 합니다.
- 3개는 서로 다른 각도여야 합니다: ①기능적 이점 ②감성적 가치 ③행동 유도.
```

| 필드 | 설명 |
|------|------|
| `text` | 슬로건 본문 |
| `angle` | `"기능"` / `"감성"` / `"행동유도"` |
| `tone_note` | 이 문구가 톤앤매너를 어떻게 반영했는지 (요건 4의 "톤앤매너에 맞는"을 **증명**하는 필드) |

> **`angle`을 스키마에 넣은 이유** — 조건 없이 3개를 요청하면 비슷한 문장 3개가 나옵니다.
> 축을 미리 나눠주면 서로 다른 3개가 나옵니다. **프롬프트에서 다양성을 요구하는 것보다
> 스키마에서 슬롯을 나누는 편이 훨씬 안정적입니다.**

### 7.4 브랜드 스토리 (`gen_story.py` — P3, 요건 5)

```
조건:
- 300자 내외(270~330자)의 한국어 산문 한 편으로 작성합니다.
- 반드시 세 가지를 포함합니다: 탄생 배경 / 브랜드 철학 / 비전.
- 소제목·불릿·번호를 쓰지 않고 이어지는 문단으로 씁니다.
- 통계나 수상 이력처럼 검증할 수 없는 사실을 지어내지 않습니다.
```

| 필드 | 설명 |
|------|------|
| `text` | 스토리 본문 |
| `background` / `philosophy` / `vision` | 각 요소 한 줄 요약 — 요건 5의 3요소 포함을 **기계적으로 확인** 가능하게 함 |

**후처리** — `char_count = len(text)`를 계산해 저장. 250자 미만 또는 400자 초과면
`errors`에 `type:"length"` 경고를 남기되 **재시도하지 않고 그대로 씁니다.**

> **글자 수가 어긋나도 재시도하지 않는 이유** — "300자 내외"는 품질 기준이지 정합성 기준이
> 아닙니다. 340자짜리 좋은 스토리를 버리고 재호출하면 쿼터만 쓰고 결과가 나빠질 수 있습니다.
> **기록은 남기되 진행합니다.**

### 7.5 컬러 팔레트 (`gen_palette.py` — P4, 요건 6)

```
조건:
- 메인 컬러 1개와 서브 컬러 3개를 제안합니다.
- 색상은 반드시 #RRGGBB 형식의 6자리 HEX 코드로만 표기합니다.
- 각 색에 한글 이름과 사용처(예: 로고, 배경, 강조)를 붙입니다.
- 메인 컬러와 서브 컬러 중 최소 하나는 흰 글자를 얹어도 읽히도록 충분히 어두워야 합니다.
```

| 필드 | 타입 | 설명 |
|------|------|------|
| `main.hex` | str | `#1B4332` |
| `main.name` | str | `"딥 포레스트"` |
| `main.usage` | str | `"로고·주요 버튼"` |
| `subs[]` | list | 같은 구조로 2~3개 |
| `rationale` | str | 이 조합을 고른 이유 |

**HEX 검증 (P4 책임 — 이 단계에서 가장 자주 깨지는 곳)**

```python
HEX = re.compile(r"^#[0-9A-Fa-f]{6}$")

def normalize_hex(value: str) -> str | None:
    v = (value or "").strip()
    if not v.startswith("#"):
        v = "#" + v                       # "1B4332" 처럼 # 없이 오는 경우
    if len(v) == 4:                        # "#1B4" 축약형 → "#11BB44"
        v = "#" + "".join(c * 2 for c in v[1:])
    return v.upper() if HEX.match(v) else None
```

| 잘못된 값 | 처리 |
|-----------|------|
| `"deep green"` (색 이름) | `None` → 기본 팔레트의 해당 위치 색으로 대체 + `errors` 기록 |
| `"#1B4"` (축약형) | 6자리로 확장해 사용 |
| `"1B4332"` (# 누락) | `#` 붙여 사용 |
| `"#1B43322"` (7자리) | `None` → 대체 |

**기본 팔레트 (전부 실패했을 때)**

```python
FALLBACK_PALETTE = {
    "main": {"hex": "#2F3E46", "name": "차콜 그레이", "usage": "로고·본문"},
    "subs": [
        {"hex": "#84A98C", "name": "세이지 그린", "usage": "강조"},
        {"hex": "#CAD2C5", "name": "라이트 세이지", "usage": "배경"},
    ],
}
```

> **기본 팔레트를 두는 이유** — `palette.png`는 요건 6의 필수 산출물입니다. LLM이 실패했다고
> PNG를 안 만들면 산출물 하나가 통째로 빕니다. 다만 **기본 팔레트를 썼다는 사실을
> `palette.is_fallback = true`로 JSON에 반드시 남깁니다.** 결과물을 속이지 않는 것이 원칙입니다.

### 7.6 경쟁사 분석 (`gen_competitor.py` — P1, 보너스 B1)

브리프에 `competitors`가 **있을 때만** 실행합니다. 없으면 `[5/7] ⚠️ 건너뜀`으로 표시하고 넘어갑니다.

```
조건:
- 각 경쟁사에 대해 추정되는 포지셔닝과 강점을 각각 한 문장으로 정리합니다.
- 확인되지 않은 매출·점유율 같은 수치를 지어내지 않습니다. 모르면 "공개 정보 부족"이라고 씁니다.
- 마지막에 이 브랜드가 취할 차별화 포인트 3개를 제안합니다.
- 차별화 포인트는 브리프의 키워드 {keywords} 중 최소 하나와 연결되어야 합니다.
```

| 필드 | 설명 |
|------|------|
| `analysis[].name` / `.positioning` / `.strength` | 경쟁사별 분석 |
| `differentiation[].point` / `.why` / `.linked_keyword` | 차별화 제안 + 연결된 브리프 키워드 |

> ⚠️ **LLM은 실존 기업에 대해 사실이 아닌 내용을 자신 있게 씁니다.** 온도를 `0.4`로 낮추고
> "모르면 모른다고 쓰라"는 지시를 넣지만, 그래도 **결과물에는 "AI 추정이며 사실 확인 필요"라는
> 문구를 JSON과 README에 함께 남깁니다.** 이건 기술 문제가 아니라 정직성 문제입니다.

### 7.7 이미지 생성 어댑터 (`image_client.py` — P5, 요건 7)

**어댑터 구조 — 제공자마다 다른 것은 4개뿐입니다.**

```python
class ImageProvider:
    def build_request(self, prompt: str, n: int) -> dict: ...
    def call(self, req: dict) -> object: ...              # SDK 호출
    def extract_images(self, resp) -> list[bytes]:  ...   # → PNG 바이트
    def credentials(self) -> str | None: ...              # 없으면 키 이름을 반환

IMAGE_PROVIDERS = {"openai": OpenAIImageProvider, "gemini": GeminiImageProvider}
```

타임아웃·재시도·오류 분기·로그·저장은 **공통 `generate_images()`에 한 번만** 둡니다.
**제공자를 추가할 때 `generate_images()`를 고치지 마십시오.**

| 제공자 | 후보 모델 | 응답 형태 | 과금 | 상태 |
|--------|-----------|-----------|------|------|
| `openai` | DALL·E 3 계열 / `gpt-image-1` 계열 | URL 또는 base64 (`response_format`로 선택) | **유료 (선불 크레딧)** | ❓ Day 1 확인 |
| `gemini` | 이미지 생성 모델 (Imagen 계열) | inline data (base64) | 무료 티어 여부 확인 필요 | ❓ Day 1 확인 |

> ⚠️ **위 표의 모델명·형식은 전부 Day 1에 실호출로 확인해야 하는 미검증 항목입니다.**
> A1-2에서 `models.list`에 있는 모델이 호출하면 `404`가 나는 사례를 겪었습니다.
> **문서에 적혀 있다는 것은 호출된다는 뜻이 아닙니다.** 확인 후 이 표를 수정하십시오.

**응답 → PNG 저장 (요건 7의 핵심)**

```python
# 형태 A — URL로 오는 경우: 다운로드해야 파일이 된다. URL은 대개 짧은 시간 뒤 만료된다.
png_bytes = requests.get(image_url, timeout=30).content

# 형태 B — base64로 오는 경우: 디코딩하면 바로 바이트다.
png_bytes = base64.b64decode(b64_string)

# 저장 — 반드시 바이너리 모드("wb"). "w"로 열면 UnicodeDecodeError가 난다.
with open(os.path.join(out_dir, f"logo_{i}.png"), "wb") as f:
    f.write(png_bytes)
```

> **URL 방식을 만나면 base64 방식으로 바꿀 수 있는지 먼저 확인하십시오.** URL은 만료되고
> 다운로드가 한 번 더 실패할 수 있는 지점입니다. **네트워크 왕복이 하나 줄면 실패 지점도 하나 줍니다.**

**저장 후 검증 (P5 책임)**

```python
# 파일이 생겼다고 성공이 아니다. 0바이트 파일이나 에러 JSON이 저장될 수 있다.
assert len(png_bytes) > 1000                    # 너무 작으면 이미지가 아님
assert png_bytes[:8] == b"\x89PNG\r\n\x1a\n"    # PNG 매직 넘버
```

> **매직 넘버를 확인하는 이유** — API가 오류를 JSON 본문으로 돌려주는데 그걸 그대로 `.png`로
> 저장하면 **파일은 생기지만 열리지 않습니다.** 제출 직전에 발견하면 손 쓸 시간이 없습니다.

### 7.8 로고 프롬프트 설계 (`gen_logo.py` — P5)

```
A minimalist vector logo for a brand.
Industry: {industry}
Brand concept: {keywords를 영문으로}
Mood: {tone을 영문으로}
Primary color: {palette.main.hex}   ← 팔레트가 있을 때만
Style: flat vector, simple geometric symbol mark, centered,
       solid white background, no text, no letters, no words.
```

| 지침 | 이유 |
|------|------|
| **`no text, no letters` 를 반드시 넣는다** | 이미지 생성 모델은 글자를 자주 뭉갭니다. 브랜드명을 넣으라고 하면 철자가 깨진 시안이 나옵니다. **심볼 마크만 받고 글자는 나중에 얹는 것이 실무 방식이기도 합니다** |
| 배경을 `solid white`로 고정 | 시안 2~3장의 배경이 제각각이면 비교가 안 됩니다 |
| `flat vector` / `centered` | 사실적 3D 렌더링이 나오면 로고로 쓸 수 없습니다 |
| 영문 프롬프트 | 이미지 모델은 대체로 영문 프롬프트에서 결과가 안정적입니다. 브리프의 한글 키워드를 **LLM으로 영문 변환**해 넣습니다 (이 변환도 실패할 수 있으므로, 실패하면 한글 그대로 사용) |
| 시안 간 변화 | 2~3장을 **완전히 같은 프롬프트로** 요청하지 않고, `symbol mark` / `abstract monogram` / `emblem` 처럼 **형식만 바꿔** 서로 다른 시안이 나오게 합니다 |

---

## 8. 컬러 팔레트 시각화 명세 (P4, 요건 6)

### 8.1 레이아웃

```
┌──────────────────────────────────────────────────────┐
│  이음 (Ieum) — 브랜드 컬러 팔레트                    │   ← 제목
│                                                      │
│  ┌────────────┐ ┌────────┐ ┌────────┐ ┌────────┐    │
│  │            │ │        │ │        │ │        │    │
│  │   MAIN     │ │  SUB1  │ │  SUB2  │ │  SUB3  │    │   ← 메인은 더 크게
│  │  #1B4332   │ │#84A98C │ │#CAD2C5 │ │#F4F1DE │    │
│  │ 딥 포레스트 │ │세이지  │ │라이트  │ │크림    │    │
│  └────────────┘ └────────┘ └────────┘ └────────┘    │
│   로고·주요버튼   강조      배경       여백          │   ← 사용처
└──────────────────────────────────────────────────────┘
```

| 항목 | 값 |
|------|-----|
| 캔버스 | `figsize=(10, 4)`, `dpi=150` → 1500×600 PNG |
| 메인 스와치 | 서브의 1.5배 폭 — **메인 컬러임을 크기로 표현** |
| 각 칸 내부 | HEX 코드 + 한글 색이름 (칸 안), 사용처 (칸 아래) |
| 축 | `ax.axis("off")` — 눈금·테두리 제거 |
| 저장 | `plt.savefig(path, dpi=150, bbox_inches="tight")` |

```python
import matplotlib
matplotlib.use("Agg")     # ★ GUI 백엔드 없이 파일만 저장. 서버·CI에서 멈추지 않음
import matplotlib.pyplot as plt
```

> **`matplotlib.use("Agg")`를 `pyplot` import 앞에 두는 이유** — 기본 백엔드는 창을 띄우려
> 시도합니다. GUI가 없는 환경에서는 여기서 멈추거나 경고가 납니다. **파일만 만들 것이므로
> 창을 띄우지 않는 백엔드를 명시**합니다. 순서가 바뀌면 적용되지 않습니다.

### 8.2 한글 폰트 (⚠️ 반드시 처리)

**matplotlib 기본 폰트에는 한글 글리프가 없어서 한글이 전부 `□□□`로 나옵니다.**

```python
from matplotlib import font_manager, rcParams

CANDIDATES = ["Malgun Gothic", "AppleGothic", "NanumGothic", "Noto Sans CJK KR"]
installed = {f.name for f in font_manager.fontManager.ttflist}
for name in CANDIDATES:
    if name in installed:
        rcParams["font.family"] = name
        break
else:
    print("⚠️  한글 폰트를 찾지 못했습니다. 팔레트 이미지의 한글이 깨질 수 있습니다.")
    # 깨지더라도 HEX 코드(영문·숫자)는 정상 출력되므로 이미지 생성은 계속합니다.

rcParams["axes.unicode_minus"] = False   # 마이너스 기호가 □로 나오는 것 방지
```

> **후보 목록으로 탐색하는 이유** — 팀원 5명의 OS가 다릅니다. Windows는 `Malgun Gothic`,
> macOS는 `AppleGothic`이 기본으로 있습니다. **한 사람의 PC에서만 되는 코드는 팀 프로젝트에서
> 버그와 같습니다.** 없으면 경고만 하고 진행하는 것도 요건 9의 정신과 같습니다.

### 8.3 글자색 자동 결정 (상대 휘도)

어두운 색 칸에 검은 글씨를 쓰면 안 보입니다. 배경 밝기에 따라 글자색을 바꿉니다.

```python
def text_color_for(hex_code: str) -> str:
    r, g, b = (int(hex_code[i:i+2], 16) / 255 for i in (1, 3, 5))
    # 사람 눈은 초록에 가장 민감하고 파랑에 둔감 → 채널마다 가중치가 다르다
    luminance = 0.299 * r + 0.587 * g + 0.114 * b
    return "#000000" if luminance > 0.55 else "#FFFFFF"
```

> **가중치가 왜 균등하지 않은가** — 같은 값의 순수 초록(`#00FF00`)과 순수 파랑(`#0000FF`)을
> 나란히 두면 초록이 훨씬 밝아 보입니다. 단순 평균 `(r+g+b)/3`을 쓰면 남색 칸에 검은 글씨가
> 얹혀 안 보이는 일이 생깁니다. **"밝기"는 물리량이 아니라 지각량**이라 가중치가 필요합니다.

### 8.4 실패 시 동작

| 상황 | 처리 |
|------|------|
| 팔레트 JSON 생성 실패 | `FALLBACK_PALETTE`로 PNG 생성 + `is_fallback: true` 기록 |
| HEX 일부만 잘못됨 | 잘못된 색만 기본값으로 대체 + `errors` 기록, 나머지는 그대로 |
| 한글 폰트 없음 | 경고 후 계속 (HEX 코드는 정상 출력됨) |
| `savefig` 실패 (권한/경로) | `errors` 기록 후 **PNG 없이 계속** — JSON 저장은 진행 |

---

## 9. 데이터 구조

### 9.1 `brand_result.json` 전체 스키마 (요건 8)

```jsonc
{
  "generated_at": "2026-08-21T14:03:11+09:00",
  "brief": { "industry": "...", "target": "...", "keywords": ["..."], "tone": "..." },

  "naming": [
    { "name_ko": "이음", "name_en": "Ieum",
      "meaning": "'잇다'의 명사형으로, 쓰고 버리는 흐름을 다시 잇는다는 뜻",
      "rationale": "키워드 '순환'을 한 단어로 압축" }
  ],

  "slogans": [
    { "text": "다시 쓰는 하루", "angle": "감성",
      "tone_note": "'따뜻하고 담백한' 톤에 맞춰 형용사를 줄이고 명사로 마무리" }
  ],

  "story": {
    "text": "...(300자 내외)...",
    "background": "...", "philosophy": "...", "vision": "...",
    "char_count": 312
  },

  "palette": {
    "main": { "hex": "#1B4332", "name": "딥 포레스트", "usage": "로고·주요 버튼" },
    "subs": [ { "hex": "#84A98C", "name": "세이지 그린", "usage": "강조" } ],
    "rationale": "...",
    "is_fallback": false
  },

  "competitors": {                        // 보너스 B1 — 없으면 null
    "analysis": [ { "name": "...", "positioning": "...", "strength": "..." } ],
    "differentiation": [ { "point": "...", "why": "...", "linked_keyword": "순환" } ],
    "disclaimer": "AI 추정 결과이며 사실 확인이 필요합니다."
  },

  "assets": {
    "palette_image": "palette.png",
    "logos": ["logo_1.png", "logo_2.png"],
    "logo_prompt": "A minimalist vector logo ..."
  },

  "errors": [
    { "step": "logo", "type": "auth", "message": "이미지 API 인증 실패 (401)",
      "at": "2026-08-21T14:03:05+09:00" }
  ],

  "meta": { "llm_model": "gemini-3.6-flash", "image_provider": "openai", "mock": false }
}
```

| 설계 결정 | 이유 |
|-----------|------|
| `brief`를 통째로 복사해 저장 | 나중에 결과만 보고 **어떤 입력에서 나온 결과인지** 알 수 있어야 함 |
| 실패한 섹션은 `null` | 키 자체를 빼면 읽는 쪽이 `KeyError`를 만납니다. **키는 있고 값이 `null`** 이 안전 |
| `errors`가 항상 존재 | 성공해도 `[]`. 있는지 없는지 확인하는 코드가 필요 없음 |
| `assets`에 **파일명만** 저장 | 절대 경로를 넣으면 다른 PC에서 무의미하고, **경로에 사용자 이름이 노출**됩니다 |
| `meta.mock` | 목업 실행 결과를 실제 결과로 착각하는 사고 방지 |

### 9.2 파일명 규칙

| 산출물 | 파일명 | 비고 |
|--------|--------|------|
| 텍스트 결과 | `brand_result.json` | **요건 8에 이름이 지정됨 — 바꾸지 말 것** |
| 팔레트 | `palette.png` | |
| 로고 | `logo_1.png`, `logo_2.png`, `logo_3.png` | 1부터 시작. **성공한 것만 번호를 매김** |

> **로고 3장 요청에 2장만 성공하면 `logo_1`, `logo_2`로 저장합니다.** `logo_1`, `logo_3`처럼
> 번호가 비면 사용자가 "2번은 어디 갔나" 하고 파일을 찾게 됩니다. **번호는 요청 순서가 아니라
> 저장 순서**입니다.

```python
json.dump(result, f, ensure_ascii=False, indent=2)
```

> `ensure_ascii=False` 없이 저장하면 한글이 `\uc774\uc74c` 형태로 들어갑니다. 파일은 유효하지만
> 사람이 열어볼 수 없어서 제출물로서 가치가 떨어집니다.

### 9.3 `errors` 항목 스키마

| 필드 | 값 |
|------|-----|
| `step` | `"brief"` `"naming"` `"slogan"` `"story"` `"palette"` `"palette_image"` `"competitor"` `"logo"` `"save"` |
| `type` | `"auth"` `"quota"` `"network"` `"parse"` `"invalid"` `"empty"` `"length"` `"skipped"` `"io"` |
| `message` | 사람이 읽을 한 줄. **예외 원문을 그대로 넣지 않음** (§11.4) |
| `at` | ISO8601 시각 |

---

## 10. 예외 처리 명세

**분류 원칙: 실패를 두 종류로 나눕니다.**

| 구분 | 정의 | 처리 |
|------|------|------|
| **치명적 (Fatal)** | **입력 자체가 없어서** 프로그램이 할 일이 남지 않는 실패 | 안내 출력 후 즉시 종료 |
| **부분 실패 (Degraded)** | 결과의 **일부만** 비는 실패 | `errors`에 기록하고 **계속 진행** |

> A1-2에서는 체인 구조 때문에 Fatal이 많았습니다. **A2-1은 단계가 서로 독립적이라 Fatal이
> 브리프 관련 3건뿐이고 나머지는 전부 Degraded입니다.** 이 차이가 요건 9의 근거입니다.

| ID | 상황 | 구분 | 처리 |
|----|------|:----:|------|
| E1 | 브리프 파일 없음 / 경로 오타 | Fatal | **재입력 루프** (§5.1). 3회 실패 시 종료 |
| E2 | 브리프 JSON 파싱 실패 | Fatal | 줄·칸 번호 안내 후 exit 1 |
| E3 | 브리프 필수 필드 누락 / 타입 오류 | Fatal | 없는 키를 모두 나열하고 exit 1 |
| E4 | `GEMINI_API_KEY` 미설정 | **Degraded** | 설정 방법 안내 + LLM 5단계를 전부 건너뛰고 **로고·저장은 진행** |
| E5 | 이미지 API 키 미설정 | Degraded | 안내 후 로고 단계만 건너뜀 |
| E6 | LLM JSON 파싱/검증 실패 | 재시도 | 프롬프트를 축약해 **1회 재시도**. 2회째도 실패하면 해당 섹션 `null` |
| E7 | LLM 호출 실패 (네트워크·타임아웃) | Degraded | 해당 섹션 `null`, 다음 단계 진행 |
| E7-a | LLM `503` (모델 과부하) | 재시도 | **3회까지 2·4초 백오프**. 일시적이라 대개 풀림 |
| E7-b | LLM `429` (무료 한도 초과) | Degraded | 한도 소진 안내. **재시도하지 않음** — 그날 안 풀림. E8과 같은 근거로 **남은 LLM 단계도 건너뜀** |
| E8 | LLM `401`/`403` (키 오류) | Degraded | "키가 잘못되었습니다" 안내 + **남은 LLM 단계도 건너뜀** (같은 키로 계속 실패할 것이므로) |
| E9 | HEX 코드 형식 오류 | Degraded | 해당 색만 기본값 대체 (§7.5) |
| E10 | 한글 폰트 없음 | 경고 | 경고 후 PNG 생성 계속 (§8.2) |
| E11 | `savefig` 실패 (권한·경로) | Degraded | `palette.png` 없이 진행 |
| E12 | 이미지 API `401`/`403` | Degraded | 키 안내, 로고 0장, 진행 |
| E13 | 이미지 API `429`/크레딧 부족 | Degraded | **"크레딧을 확인하세요" 명시** (429가 쿼터가 아니라 잔액 문제일 수 있음), 로고 0장 |
| E14 | 이미지 다운로드 실패 (URL 만료 등) | Degraded | 성공한 장수만 저장 |
| E15 | 응답이 유효한 PNG가 아님 (매직넘버·크기 불일치) | Degraded | **저장 전에 검증해 아예 만들지 않음** + `errors` 기록. 다른 이미지 형식이면 Pillow가 있을 때만 PNG로 변환 |
| E16 | 출력 폴더 생성 실패 | Fatal | 저장할 곳이 없으면 산출물이 하나도 안 남음 → exit 1 |

> **E4를 Fatal로 하지 않는 이유** — LLM 키가 없어도 요건 8("출력 폴더에 저장")은 지킬 수 있고,
> 사용자는 **"키만 넣으면 되는구나"라는 정보를 결과 파일로도 받게 됩니다.** 반면 E16은
> 저장할 폴더 자체가 없어 산출물이 0개가 되므로 Fatal입니다.

> **E8이 "남은 단계도 건너뛰는" 이유** — 키가 틀렸으면 다음 4번의 호출도 100% 같은 이유로
> 실패합니다. 4번을 더 호출하는 것은 사용자를 40초 더 기다리게 할 뿐입니다.
> **같은 원인으로 반드시 실패할 호출은 미리 접습니다** (`llm_disabled` 플래그).

### 예외를 잡는 범위

```python
# ❌ 너무 넓다 — 오타(NameError)까지 삼켜서 디버깅이 불가능해진다
try:
    ...
except Exception:
    pass

# ✅ 예상한 예외를 종류별로 구분한다
try:
    resp = client.models.generate_content(...)
except TimeoutError:                     # E7
except (ConnectionError, OSError):       # E7
except APIStatusError as e:              # e.status_code로 401/429/503 분기 (E7-a·b, E8)
except (ValueError, KeyError, TypeError):# E6 — 파싱·스키마
```

> **`except Exception: pass`가 이번 과제에서 특히 위험한 이유** — 요건 9가 "실패해도 계속
> 진행"이라 넓게 잡고 싶은 유혹이 큽니다. 하지만 그러면 P3가 만든 오타 하나가
> "API 실패"로 둔갑해 조용히 넘어가고, **아무도 원인을 못 찾습니다.**
> 계속 진행하는 것과 원인을 감추는 것은 다릅니다.

---

## 11. 보안 — API 키 관리 (요건 10)

### 11.1 왜 코드에 쓰지 않는가

| 이유 | 설명 |
|------|------|
| 공유 사고 방지 | 저장소를 공개하거나 코드를 팀원에게 보내는 순간 키가 함께 나갑니다 |
| 교체 용이 | 키를 갱신할 때 코드를 고치고 다시 커밋할 필요가 없습니다 |
| 과금 사고 예방 | 유출된 키로 제3자가 쿼터를 소진하면 **비용이 내 계정에 청구**됩니다 (이미지 API는 유료라 더 직접적) |
| **팀 프로젝트 특유** | 5명이 각자 다른 키를 쓰므로, 키가 코드에 있으면 **매번 남의 키로 덮어쓰는 커밋**이 발생합니다 |

> ⚠️ **한 번 커밋된 키는 파일에서 지워도 남습니다.** Git은 이력을 보존하므로 이전 커밋을
> 되짚으면 그대로 보입니다. 실수로 커밋했다면 **파일 수정이 아니라 키 재발급(폐기)** 이 정답입니다.

### 11.2 키 목록

| 환경변수 | 용도 | 없을 때 |
|----------|------|---------|
| `GEMINI_API_KEY` | LLM 5단계 | 안내 후 LLM 단계 전체 건너뜀 (E4) |
| `IMAGE_PROVIDER` | `openai` \| `gemini` (기본 `openai`) | 기본값 사용 |
| `OPENAI_API_KEY` | `IMAGE_PROVIDER=openai`일 때 | 로고 단계 건너뜀 (E5) |
| `USE_MOCK` | `1`이면 목업 (기본 `0`) | 실호출 |

### 11.3 설정 방법 (README에도 동일 수록)

```bash
cp .env.example .env
```

`.env` 파일에 값을 채웁니다. `.gitignore`에 `.env`가 들어 있는지 **먼저** 확인하십시오.

```python
from dotenv import load_dotenv
load_dotenv()                       # .env를 환경변수로 읽어들임
key = os.getenv("GEMINI_API_KEY")   # ← 코드에는 "이름"만 존재
```

### 11.4 유출 방지 점검 항목 (제출 전 필수)

| ✅ | 항목 |
|:--:|------|
| ☐ | `.gitignore`에 `.env`가 있는가 |
| ☐ | `git log -p \| grep -i "api_key"` 로 이력에 키가 없는지 확인했는가 |
| ☐ | 진행 로그에 **요청 URL·헤더**를 출력하지 않는가 |
| ☐ | `errors[].message`에 **예외 원문을 통째로** 넣지 않았는가 (요청 정보가 딸려옴) |
| ☐ | `brand_result.json`에 키·절대경로·사용자명이 없는가 |
| ☐ | **스크린샷에 키가 찍히지 않았는가** (새 터미널에서 촬영) |
| ☐ | README·PRD 본문에 실제 키 문자열이 없는가 |

---

## 12. 목업 모드 (§3.3 규칙 2)

`USE_MOCK=1`이면 `llm.py`와 `image_client.py`가 **네트워크를 타지 않고** 고정 응답을 돌려줍니다.

| 용도 | 설명 |
|------|------|
| **병렬 개발** | P1은 P2의 LLM 구현을 기다리지 않고 파이프라인·저장을 완성할 수 있음 |
| **쿼터 절약** | 5명이 하루 20회씩 호출하면 무료 한도가 오전에 끝남 |
| **오류 경로 테스트** | `USE_MOCK=fail_naming` 처럼 **특정 단계만 실패시켜** 요건 9를 재현. 실제 API로는 401을 마음대로 만들 수 없음 |
| **시연 안전망** | 발표 당일 네트워크가 막혀도 시연 가능 |

```python
MOCK_MODES = {
    "1":            "전 단계 정상 응답",
    "fail_naming":  "네이밍만 실패 → 슬로건·스토리·컬러·로고는 정상 (요건 9 증명)",
    "fail_llm":     "LLM 전체 실패 → 로고와 저장만 진행 (E4 재현)",
    "fail_image":   "이미지만 실패 → 텍스트·팔레트는 정상 (E12 재현)",
    "bad_hex":      "HEX에 'deep green' 반환 → 기본 팔레트 대체 확인 (E9 재현)",
}
```

> **`fail_*` 모드가 이 프로젝트에서 가장 값어치 있는 코드입니다.** 요건 9는 "실패해도 계속
> 진행한다"인데, **실패를 만들 수 없으면 그 요건을 만족했는지 증명할 수 없습니다.**
> 발표에서 "이렇게 동작합니다"를 보여줄 때도 이 모드를 씁니다.

---

## 13. 7일 실행 계획 (5인 병렬)

> 날짜는 2026-08-21(금) 시작 기준입니다. 실제 마감일에 맞춰 조정하십시오.
> **Day 1과 Day 2가 이 프로젝트의 성패를 가릅니다.** 여기서 인터페이스가 안 나오면 병렬이 안 됩니다.

### Day 1 — 8/21(금) 킥오프 · 환경 · 키 ★가장 중요

| 담당 | 할 일 |
|:----:|-------|
| 전원 | PRD 함께 읽기(30분) → **§0 확정 사항과 §3 역할에 합의**. 저장소·브랜치 생성 |
| 전원 | `pip install -r requirements.txt`, `.env.example` 복사, **각자 Gemini 키 발급 + 1회 호출 성공 확인** |
| **P5** | ⭐ **이미지 API 실호출 1회** — 제공자·모델명·응답 형식(URL/base64)·과금 확인 후 **§7.7 표를 수정** |
| P1 | `.gitignore` 먼저 커밋(`.env` 포함), 폴더 구조 생성 |
| P4 | `matplotlib` 설치 + **한글 폰트 확인**(`□□□` 여부를 실제 PNG로) |

> **Day 1 종료 조건: 5명 전원이 "내 PC에서 LLM 호출 1회 성공"을 확인했고, P5가 이미지 제공자를
> 확정했다.** 이게 안 되면 Day 2로 넘어가지 말고 Day 1을 연장하십시오.

### Day 2 — 8/22(토) 인터페이스 동결 ★

| 담당 | 할 일 |
|:----:|-------|
| **P2** | ⭐ `llm.py` 시그니처 + `mock.py` 골격 완성 → **팀에 공유하고 동결** |
| 전원 | `brand_result.json` 스키마(§9.1)를 **함께 확정**. 각자 자기 섹션 필드명 결정 |
| P1 | `main.py` 대화형 입력 + `brief.py` 검증 |
| P3·P4·P5 | 프롬프트 초안 작성 (아직 호출하지 않음) |

> **동결 회의는 30분을 넘기지 마십시오.** 완벽한 스키마를 만들려다 하루를 쓰는 것보다,
> 80% 스키마를 확정하고 Day 4 통합에서 고치는 편이 빠릅니다.

### Day 3 — 8/23(일) 각자 모듈 (목업 위에서)

| 담당 | 할 일 |
|:----:|-------|
| P2 | `llm.py` 실구현 — JSON 강제·재시도·오류 분류 (§7.1) |
| P3 | `gen_naming.py` (다국어 포함) + `gen_slogan.py` |
| P4 | `gen_palette.py` HEX 검증 + `viz_palette.py` 뼈대 |
| P5 | `image_client.py` 어댑터 + **PNG 저장·매직넘버 검증** |
| P1 | `storage.py`, `logger.py`, 파이프라인 골격 (전부 목업으로 연결) |

> **이날 저녁 `USE_MOCK=1 python main.py`가 끝까지 돌아야 합니다.** 내용이 전부 가짜여도
> **파이프라인이 통째로 도는 상태**를 Day 3에 확보하는 것이 목표입니다.

### Day 4 — 8/24(월) 1차 실키 통합 ⭐ 실패가 가장 많은 날

| 담당 | 할 일 |
|:----:|-------|
| 전원 | `USE_MOCK=0`으로 **전 단계 실호출**. 여기서 처음으로 진짜 오류들이 나옴 |
| P3 | 프롬프트 튜닝 — 개수 부족, 톤 미반영, 스토리 길이 |
| P4 | 실제 LLM이 준 HEX로 PNG 생성 — **폰트·대비 문제가 여기서 드러남** |
| P5 | 실제 로고 생성 — **글자 뭉개짐 확인 후 `no text` 지시 강화** |
| P1 | 스키마 불일치 수정 (필드명 오타·타입 차이가 반드시 나옴) |

### Day 5 — 8/25(화) 오류 처리 · 보너스

| 담당 | 할 일 |
|:----:|-------|
| P2 | E6~E8 재시도·백오프 완성, `fail_*` 목업 모드 구현 |
| P1 | 경쟁사 분석(B1) + 진행 로그 다듬기 (§5.2 화면대로) |
| P3 | 후처리(개수·글자수 보정), 다국어 네이밍(B2) 마무리 |
| P4·P5 | 실패 폴백 (기본 팔레트, 로고 0장 처리) |

### Day 6 — 8/26(수) 통합 테스트 · 문서

| 담당 | 할 일 |
|:----:|-------|
| 전원 | **T1~T16 전체 실행** (§14.1). 담당별로 나눠서 |
| P1 | README 작성 — 기능표 + 스크린샷 + **"설계 결정 (왜 이렇게 만들었는가)"** |
| 전원 | §11.4 **키 유출 점검 전 항목** 통과 |
| 전원 | **발표 리허설 1회** (§설명가이드) |

### Day 7 — 8/27(목) 마무리 · 제출

| 담당 | 할 일 |
|:----:|-------|
| P1 | `develop` → `main` 병합, 최종 산출물 확인 (§15) |
| 전원 | 스크린샷 재촬영(키 안 찍히게), 결과물 파일 열어보기 |
| 전원 | 발표 최종 리허설 |

> **Day 7에 새 기능을 넣지 마십시오.** 이날 추가된 코드가 제출본을 깨뜨리는 것이
> 팀 프로젝트에서 가장 흔한 사고입니다.

---

## 14. 검증

### 14.1 수동 테스트 시나리오

| ID | 시나리오 | 기대 결과 | 담당 | API |
|----|----------|-----------|:----:|:---:|
| T1 | 없는 경로 입력 | 안내 후 **재입력 요구** (종료 아님) | P1 | ❌ |
| T2 | 경로에 큰따옴표 포함 | 정상 인식 (§5.1) | P1 | ❌ |
| T3 | 깨진 JSON 파일 | **줄·칸 번호** 안내 후 종료 | P1 | ❌ |
| T4 | `keywords` 누락 | 누락 키 이름 출력 후 종료 | P1 | ❌ |
| T5 | `tone`이 리스트 | ⚠️ 경고 후 **계속 진행** | P1 | ❌ |
| T6 | 출력 폴더 미입력(Enter) | `./output` 생성 | P1 | ❌ |
| T7 | 정상 브리프 전체 실행 | JSON + palette.png + logo 2장 | 전원 | ✅ |
| T8 | `GEMINI_API_KEY` 미설정 | 안내 + LLM 건너뜀, **JSON은 저장됨** | P2 | ❌ |
| T9 | 잘못된 키 (한 글자 변경) | 401 안내 + **남은 LLM 단계 자동 건너뜀** (E8) | P2 | ✅ |
| T10 | `USE_MOCK=fail_naming` | **네이밍만 null, 나머지 정상** ⭐요건 9 증명 | P2 | ❌ |
| T11 | `USE_MOCK=fail_image` | 로고 0장, 텍스트·팔레트 정상 | P5 | ❌ |
| T12 | `USE_MOCK=bad_hex` | 기본 팔레트 대체 + `is_fallback:true` | P4 | ❌ |
| T13 | 네이밍 2개만 반환되도록 유도 | 1회 재시도 후 3개 이상 확보 | P3 | ✅ |
| T14 | 생성된 `palette.png` 육안 확인 | **한글 안 깨짐**, 어두운 칸에 흰 글자 | P4 | ❌ |
| T15 | 생성된 `logo_*.png` 열기 | 실제로 열리고, **글자가 없는 심볼** | P5 | ✅ |
| T16 | `competitors` 없는 브리프 | `[5/7] ⚠️ 건너뜀` 표시, `competitors: null` | P1 | ✅ |

> **API ❌ 표시는 실호출 없이 검증 가능한 항목입니다 — 16개 중 11개.**
> 쿼터를 아끼면서도 대부분을 검증할 수 있게 설계했습니다.

### 14.2 스크린샷 계획 (`docs/screenshots/`)

| 파일 | 내용 |
|------|------|
| `01_실행화면.png` | 대화형 입력 + 진행 로그 전체 (성공) |
| `02_부분실패.png` | `fail_image` — 로고 실패했지만 나머지 저장됨 ⭐ |
| `03_키없음.png` | 키 미설정 안내 메시지 |
| `04_브리프오류.png` | 필수 필드 누락 안내 |
| `05_팔레트.png` | 생성된 `palette.png` |
| `06_로고.png` | 로고 시안 2~3장 나란히 |
| `07_결과JSON.png` | `brand_result.json` 열어본 화면 |

> ⚠️ **모든 스크린샷은 새 터미널에서 촬영합니다.** 이전 명령 이력에 키가 남아 있을 수 있습니다.

---

## 15. 제출물 체크리스트

| ✅ | 항목 | 요건 |
|:--:|------|:----:|
| ☐ | `main.py` + `brandkit/` 실행 가능 | 전체 |
| ☐ | 대화형 입력 동작 (`print`/`input`) | 1 |
| ☐ | 브리프 필수/선택 필드 처리 | 2 |
| ☐ | 네이밍 3~5개 + 의미 | 3 |
| ☐ | 슬로건 3개 | 4 |
| ☐ | 스토리 300자 내외 (배경·철학·비전) | 5 |
| ☐ | 컬러 메인1+서브2~3 HEX + **`palette.png`** | 6 |
| ☐ | **`logo_1.png` 이상 2~3장, 실제로 열림** | 7 |
| ☐ | **`output/brand_result.json`** (파일명 정확히) | 8 |
| ☐ | 실패해도 다음 단계 진행 (T10 스크린샷으로 증명) | 9 |
| ☐ | 코드에 키 없음 + `.env.example` 존재 | 10 |
| ☐ | 경쟁사 분석 동작 | B1 |
| ☐ | 한글+영문 네이밍 동시 출력 | B2 |
| ☐ | `README.md` (기능표 + 스크린샷 + 설계 결정) | — |
| ☐ | `requirements.txt` | — |
| ☐ | §11.4 키 유출 점검 전 항목 통과 | 10 |
| ☐ | 5명 전원 커밋 이력 존재 | 팀 |

---

## 16. 과제 목표 대응 — "설명할 수 있어야 한다"

과제 §3이 요구하는 4가지입니다. **자세한 설명 대본은 `설명가이드.md`에 있습니다.**

| 목표 | 근거 위치 | 한 줄 답안 |
|------|-----------|-----------|
| 브리프 → AI 브랜드 요소 생성 파이프라인 | §4.1, §6 | 브리프 JSON을 검증해 딕셔너리로 만들고, 그 값을 프롬프트 템플릿에 채워 단계별로 LLM을 호출한 뒤, 각 응답을 결과 JSON의 자기 자리에 모은다. 단계가 서로 독립적이라 하나가 실패해도 나머지가 남는다. |
| LLM API + 이미지 API 조합 | §7.1, §7.7, §7.8 | LLM은 **텍스트(JSON)** 를 돌려주고 이미지 API는 **바이너리(URL 또는 base64)** 를 돌려준다. LLM 결과(브랜드명·HEX)를 이미지 프롬프트의 재료로 넘겨 두 API를 연결하고, 응답은 `wb` 모드로 PNG 저장한다. |
| 컬러 팔레트 시각화 | §8 | LLM에게 HEX로만 답하게 강제하고, 정규식으로 검증한 뒤 matplotlib 사각형으로 그려 `savefig`한다. 한글 폰트 지정과 배경 밝기에 따른 글자색 전환이 핵심이다. |
| API 오류 상황과 대응 | §10, §12 | 인증(401)·쿼터(429)·네트워크(타임아웃)·파싱(JSON) 네 가지다. **입력이 사라지면 중단하고, 결과 일부만 비면 계속 진행한다.** 재시도는 성격에 따라 다르게 — 503은 백오프 재시도, 429는 재시도하지 않는다. |

---

## 17. 리스크 & 대응

| 리스크 | 심각도 | 대응 |
|--------|:------:|------|
| **이미지 API 키 발급·과금 지연** | 🔴 최상 | Day 1 최우선. 어댑터로 제공자 교체 가능(§0). 최악의 경우 P5 키 1개 공용 |
| ~~P2의 `llm.py`가 Day 2에 안 나옴~~ | 🔴 최상 | ✅ **해소** — 시그니처 동결 + 목업 우선으로 구현 완료. 인터페이스와 구현을 분리한 것이 해법이었음 |
| 이미지 모델명이 문서와 다름 | 🟠 상 | A1-2 사례 — `models.list`에 있어도 404. Day 1 실호출로 확인 |
| **Gemini 무료 한도 소진 (5명분)** | 🟠 상 | 실행 1회에 5~6회 호출 → 하루 3~4회분. **`USE_MOCK` 기본값을 1로** 두고 실호출은 의도적으로만 |
| 로고에 글자가 뭉개져 나옴 | 🟠 상 | 프롬프트에 `no text, no letters` (§7.8). 심볼 마크만 받기 |
| ~~matplotlib 한글 □□□~~ | 🟠 상 | ✅ **해소** — 폰트 후보 탐색 구현, 실제 PNG로 한글 정상 출력 확인 (§8.2) |
| 팀원 간 스키마 필드명 불일치 | 🟠 상 | Day 2 동결(§3.3). Day 4 통합에서 반드시 한 번은 터지므로 **하루를 미리 잡아둠** |
| 병합 충돌로 코드 유실 | 🟡 중 | 파일 소유권(§3.1) + `--no-ff` + PR 리뷰 |
| `except Exception: pass`로 오타가 숨음 | 🟡 중 | 예외 종류별 분기 (§10). 코드 리뷰 체크 항목 |
| 저장된 PNG가 안 열림 | 🟡 중 | ✅ **구현됨** — 저장 전 매직넘버·크기 검증 (E15) |
| **`.env` 커밋** | 🔴 최상 | Day 1에 `.gitignore` **먼저** 커밋. 사고 시 파일 수정이 아니라 **키 재발급** |
| 스크린샷에 키가 찍힘 | 🟠 상 | 새 터미널에서 촬영 (§14.2) |
| ~~콘솔 한글 깨짐 (cp949)~~ | 🟢 하 | ✅ **해소** — `sys.stdout`/`sys.stderr` 둘 다 UTF-8 고정. 미적용 시 깨짐을 측정으로 재현 확인 |
| 한글이 JSON에 `\uXXXX`로 저장 | 🟢 하 | `ensure_ascii=False` (§9.2) |
| 발표 당일 네트워크 차단 | 🟡 중 | `USE_MOCK=1`로 시연 + 미리 만든 산출물 준비 (§12) |
| 1인에게 작업 몰림 | 🟡 중 | 데일리 스탠드업에서 조기 발견 (§3.5). 보너스는 언제든 버릴 수 있게 마지막에 배치 |

---

## 부록 A. 프로그램 뼈대 (Day 2 시작점)

```python
"""브랜드 아이덴티티 생성기 (Brand Kit Generator) — 2026 AI활용학습 A2-1"""
import sys, os, json, datetime

sys.stdout.reconfigure(encoding="utf-8")   # cp949 콘솔에서 이모지·한글 깨짐 방지
sys.stderr.reconfigure(encoding="utf-8")

from brandkit import brief, storage, logger, llm
from brandkit import gen_naming, gen_slogan, gen_story, gen_palette
from brandkit import gen_competitor, viz_palette, gen_logo


def main() -> None:
    brief_path, out_dir, logo_n = ask_inputs()          # P1 §5.1

    data = brief.load_and_validate(brief_path)          # 실패 시 내부에서 종료 (Fatal)
    storage.prepare(out_dir)                            # E16
    brief.print_summary(data)

    errors: list = []                                   # ★ 모든 모듈이 공유하는 오류 목록
    result = {
        "generated_at": datetime.datetime.now().astimezone().isoformat(timespec="seconds"),
        "brief": data,
        "naming": None, "slogans": None, "story": None,
        "palette": None, "competitors": None,
        "assets": {"palette_image": None, "logos": [], "logo_prompt": None},
        "errors": errors,
        "meta": {"llm_model": llm.MODEL,
                 "image_provider": os.getenv("IMAGE_PROVIDER", "openai"),
                 "mock": os.getenv("USE_MOCK", "0") != "0"},
    }

    # ── 생성 단계: 각 호출은 실패해도 예외를 던지지 않는다 (§4.3 계약) ──
    result["naming"]  = gen_naming.generate(data,  errors=errors)     # [1/7]
    result["slogans"] = gen_slogan.generate(data,  errors=errors,
                                            naming=result["naming"])  # [2/7]
    result["story"]   = gen_story.generate(data,   errors=errors)     # [3/7]
    result["palette"] = gen_palette.generate(data, errors=errors)     # [4/7]

    if data.get("competitors"):                                       # [5/7] B1
        result["competitors"] = gen_competitor.generate(data, errors=errors)
    else:
        logger.step_skip("[5/7] 경쟁사 분석", "브리프에 competitors 없음")

    if result["palette"]:                                             # [6/7]
        result["assets"]["palette_image"] = viz_palette.save(
            result["palette"], out_dir,
            title=pick_title(result),      # 네이밍 1번 후보를 제목으로. 없으면 industry
            errors=errors)

    logos, prompt = gen_logo.generate(                                # [7/7] 소프트 의존
        data, out_dir, n=logo_n,
        naming=result["naming"], palette=result["palette"], errors=errors)
    result["assets"]["logos"] = logos
    result["assets"]["logo_prompt"] = prompt

    storage.save_json(result, out_dir)          # ★ 무조건 실행
    logger.print_summary(result, out_dir, errors)


if __name__ == "__main__":
    main()
```

> **`main()`을 읽으면 파이프라인 전체가 보입니다.** 각 줄이 §4.1의 흐름도 한 칸에 대응하고,
> 담당자와 파일이 §3.1 표에 대응합니다. **팀 프로젝트에서 이 함수 하나가 "우리가 뭘 만들었는지"에
> 대한 공통 언어**가 됩니다. 발표에서도 이 함수를 띄워놓고 설명하십시오.

---

## 부록 B. `briefs/sample_brief.json`

```json
{
  "industry": "친환경 리필 생활용품",
  "target": "환경에 관심 있는 20~30대 1인 가구",
  "keywords": ["지속가능", "미니멀", "순환", "일상"],
  "tone": "따뜻하고 담백한, 설교하지 않는",
  "competitors": ["알맹상점", "지구샵"],
  "notes": "영문 표기를 함께 제안해 주세요. 과장된 환경 구호는 피해 주세요."
}
```

**테스트용 최소 브리프** (`briefs/minimal_brief.json`) — 선택 필드가 전부 없는 경우:

```json
{
  "industry": "동네 서점",
  "target": "30~40대 직장인",
  "keywords": ["휴식", "발견"]
}
```

---

## 부록 C. 설정 파일

**`.env.example`** (커밋함 — 키 "이름"만 알려주는 견본)

```
# 실제 키를 여기에 쓰지 마십시오. 이 파일을 .env로 복사한 뒤 값을 채웁니다.
#   cp .env.example .env

# --- LLM (필수) ---
GEMINI_API_KEY=

# --- 이미지 생성 제공자 선택 (선택, 기본값 openai) ---
# openai | gemini
IMAGE_PROVIDER=

# IMAGE_PROVIDER=openai 일 때 필요
OPENAI_API_KEY=

# --- 개발용 (선택) ---
# 1 | fail_naming | fail_llm | fail_image | bad_hex   (기본 0 = 실호출)
USE_MOCK=
```

**`.gitignore`** (Day 1에 **가장 먼저** 커밋)

```
.env
__pycache__/
*.pyc
.venv/
```

**`requirements.txt`**

```
google-genai
openai
requests
python-dotenv
matplotlib
Pillow
```

> `output/`은 **제외하지 않습니다.** 결과물 제출이 요건이므로 커밋합니다.
> 단 커밋 전에 §11.4 점검 항목으로 키·절대경로가 섞이지 않았는지 확인하십시오.
